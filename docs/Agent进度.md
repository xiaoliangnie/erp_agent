# Agent 系统完成度

> **改能力必须改本文。** 完成、部分完成、新增、取消或明确不做某一项时，同步改：条目状态、百分比、分组小计、文首总进度、文末「最近变更」。
> 口径与执行顺序仍以仓库根 `README.md`、`docs/开发.md` 为准；本文只回答「Agent 现在到哪了」。

| 项 | 值 |
|---|---|
| 统计日 | 2026-08-17 |
| **总进度** | **63%** |
| 算法 | 计入「完成 / 部分 / 骨架 / 未做」；「不做」不进分母。条目等权，再按分组加权（见下） |

**怎么改进度**

1. 在对应分组表改该行的状态和 `%`。
2. 重算该分组小计（组内条目等权平均，四舍五入到整数）。
3. 按文首权重重算总进度。
4. 在「最近变更」加一行：日期、条目、从 → 到、原因。
5. 新增能力先加一行再标状态，不要只改总进度。

状态：`完成` 已闭环可用 · `部分` 主路径能用但仍有缺口 · `骨架` 代码在、默认关或缺数据 · `未做` · `不做` 本期不做（不扣分）

---

## 总览

| 分组 | 权重 | 小计 | 一句话 |
|---|---:|---:|---|
| A 内核（注册表 / 确认 / 会话 / 双通道） | 20% | 96% | 对话内核可用 |
| B 只读查询工具 | 20% | 100% | 采购、订单、国标、缺口都能查 |
| C 写操作与 ERP | 20% | 65% | 合同和鞋垫能确认后写；普通换货仍要换货页 |
| D 钉钉与意图 | 20% | 63% | 对话、催办、品控、鞋垫意图可用；写完会通知；多群和权限表未做 |
| E 身份与权限 | 10% | 56% | 绑定 + viewer/operator；无 users / 权限表 |
| F 预测与订货 | 5% | 37% | 接口和工具在，缺销售/库存表和正式模型 |
| G 工作台与后置 | 5% | 0% | `/workbench`、Impact、Outbox 未做 |
| **合计** | **100%** | **63%** | `0.20×96 + 0.20×100 + 0.20×65 + 0.20×63 + 0.10×56 + 0.05×37 + 0.05×0` |

---

## A 内核

| ID | 条目 | 状态 | % | 代码 / 说明 |
|---|---|---|---:|---|
| A1 | 工具注册表（名称 / Schema / 风险 / handler） | 完成 | 100 | `backend/agent/tools.py` |
| A2 | L0 直接执行 | 完成 | 100 | `runner._invoke` |
| A3 | L1/L2 pending 确认（谁发起谁确认、幂等一次） | 完成 | 100 | `actions.py`；网页和钉钉「确认 编号」；鞋垫也可只回「确认」 |
| A4 | 运行 / 工具 / 确认审计 | 完成 | 100 | `audit.py` |
| A5 | 会话隔离、同会话串行、空闲翻篇 | 完成 | 100 | `sessions.py` |
| A6 | 网页 `/chat` | 部分 | 90 | 可用；网页 `user_id` 仍靠署名解析 |
| A7 | CLI 调试（不经 HTTP） | 完成 | 100 | `scripts/run_agent_cli.py` |
| A8 | 黄金对话回放 | 部分 | 70 | `tests/fixtures/golden_dialogues.json` + `test_agent`；覆盖未随鞋垫意图补全 |
| A9 | 工具结果信封（摘要进模型、全量给页面） | 完成 | 100 | L0 `ok/summary/data/truncated/hint` |
| A10 | 上下文预算 / 会话摘要 | 部分 | 80 | 预算已做；摘要默认关（`AGENT_SUMMARY_ENABLED`） |

**A 小计 96%**（10 条等权）

---

## B 只读查询工具

| ID | 条目 | 状态 | % | 工具 |
|---|---|---|---:|---|
| B1 | 搜采购单 | 完成 | 100 | `search_purchase_orders` |
| B2 | 采购单头+明细 | 完成 | 100 | `get_purchase_order` |
| B3 | 四波交期催办清单 | 完成 | 100 | `delivery_reminders` |
| B4 | 看板汇总 | 完成 | 100 | `dashboard_summary` |
| B5 | 商品主数据搜索 | 完成 | 100 | `search_products` |
| B6 | 国标目录状态 | 完成 | 100 | `gb_catalog_status` |
| B7 | 执行标准查询 | 完成 | 100 | `lookup_gb_standards` |
| B8 | 主数据缺口 | 完成 | 100 | `master_data_gaps` |
| B9 | 销售订单定位（o_id） | 完成 | 100 | `search_sales_orders` |
| B10 | 销售订单明细 | 完成 | 100 | `get_sales_order_items` |

**B 小计 100%**

---

## C 写操作与 ERP

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| C1 | 生成采购合同（确认后出 Excel/预览） | 部分 | 90 | `generate_purchase_contract`；soffice 超时等未收口 |
| C2 | 普通换货 dry-run 登记 | 部分 | 90 | `submit_exchange_dry_run`；确认只登记试算 |
| C3 | 换货页二次确认 + userscript 写入 | 部分 | 80 | `/exchange` + `exchange-worker.user.js` |
| C4 | 抖音换鞋垫：定位 → 清单 → 确认 → 串行写 | 部分 | 95 | 钉钉回「确认」由后端写；写完发【任务完成】；半码舍去小数；写后回写镜像并排除已写入单；Delivering 不处理 |
| C5 | 普通换货从钉钉直接写入（不经换货页） | 未做 | 0 | 仍要 `/exchange` 二次确认 |
| C6 | Digital Worker 领取换货队列 | 骨架 | 40 | `DigitalWorkerLoop`；`ERP_AI_ENABLED` 默认关。鞋垫确认走同一 `DigitalRuntime` 锁，不要求开领取循环 |
| C7 | 写入回读 / 预览不一致则停 | 骨架 | 40 | 鞋垫执行有 after SKU；正式 before/after/trace 未收口 |
| C8 | 备注异常 / 超卖 / 改地址 | 不做 | — | 第一期只做 SKU 换货 |
| C9 | 正式创建采购单 | 不做 | — | 本期最多草稿预览，未开闸 |

**C 小计 65%**（C1–C7，不含不做）

---

## D 钉钉与意图

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| D1 | Stream 长连对话 | 部分 | 90 | `DINGTALK_ENABLED`；需 `dingtalk-stream` |
| D2 | 绑定采购员姓名 | 完成 | 100 | 「绑定 利特」；花名互认 |
| D3 | 「确认 / 取消」 | 完成 | 100 | 可带编号，也可只回「确认」执行当前会话最近一条 |
| D4 | 抖音换鞋垫固定意图 | 部分 | 95 | 查询列出；「进行处理」进确认；重复调用复用同一条 |
| D5 | 每日交期催办推送 | 部分 | 90 | 不经 LLM；`DINGTALK_REMINDER_ENABLED` |
| D6 | 品控登记 / 查询 / 日报 | 部分 | 90 | 正则直登 + 可选日报 |
| D7 | 通用意图识别（多业务） | 骨架 | 20 | 仅鞋垫一条；其它仍 LLM 选工具 |
| D8 | 多群路由（协同群 / 异常群） | 未做 | 0 | S4 |
| D9 | 出站投递台账（失败可补发） | 未做 | 0 | S2 Outbox |
| D10 | 钉钉卡片轻确认 | 未做 | 0 | 现在是纯文本「确认」，不做 ActionCard |
| D11 | 鞋垫写入完成通知 | 完成 | 100 | 确认后先回「已开始」；写完 `reply_text` 发【任务完成】并 @ 发起人 |

**D 小计 63%**

---

## E 身份与权限

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| E1 | `staff_bindings`（姓名 ↔ 钉钉 userId） | 部分 | 80 | 身份枢纽；不是账号表 |
| E2 | Web / 钉钉同一 `user_id` | 部分 | 90 | `RequestContext` |
| E3 | `viewer` / `operator` | 部分 | 80 | viewer 不能确认写操作 |
| E4 | 用户权限表 / capabilities | 骨架 | 15 | `permissions.py` 已留钩子；表未建。钉钉写操作先要求绑定 |
| E5 | 独立 `users` 表 | 未做 | 0 | S1 |
| E6 | 店铺权限矩阵 | 不做 | — | 本期不做 |
| E7 | 操作员长期记忆 | 骨架 | 30 | 「记住 / 忘记」；`AGENT_MEMORY_ENABLED` 默认关 |

**E 小计 56%**（E1–E5、E7）

---

## F 预测与订货

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| F1 | `Forecaster` 接口 + Baseline | 完成 | 100 | `backend/forecast/` |
| F2 | `forecast_demand` 工具 | 部分 | 80 | 已注册；无工件则不可用 |
| F3 | `order_suggestion` 工具 | 部分 | 80 | 库存缺失直接报错，不用 0 兜底 |
| F4 | 销售出库表进镜像 | 未做 | 0 | `FORECAST_SALES_*` 仅配置 |
| F5 | 现势库存表进镜像 | 未做 | 0 | `FORECAST_INVENTORY_*` 仅配置 |
| F6 | 拆出 `forecast_sales` + `forecast_id` | 未做 | 0 | F3，Gate A 之后 |
| F7 | 业务可用的训练工件 | 骨架 | 20 | 可 `--csv` 训练；生产模型未接入 |

**F 小计 37%**

---

## G 工作台与后置（不挡主线）

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| G1 | `/workbench` 待办列表 | 未做 | 0 | S2 |
| G2 | 换货 Impact Analysis | 未做 | 0 | S2 |
| G3 | 长任务 Job 队列 | 未做 | 0 | S2 |
| G4 | 钉钉 Outbox | 未做 | 0 | 与 D9 同一缺口 |
| G5 | `supplier_scorecard` | 不做 | — | `RESERVED_TOOLS` 占位 |
| G6 | 代发只读 / 采购草稿 / 知识库 | 不做 | — | S4，单独开闸 |

**G 小计 0%**（G1–G4）

---

## 最近变更

| 日期 | 条目 | 从 → 到 | 原因 |
|---|---|---|---|
| 2026-08-17 | 本文 | — → 首版 | 建立 Agent 完成度进度表 |
| 2026-08-17 | C4 / D4 / E4 | 未做 → 部分/骨架 | 钉钉换鞋垫：定位清单 + 二次确认 + 串行写入；意图与权限钩子落地 |
| 2026-08-17 | C4 / D3 / D4 | 部分 90 → 95；D3 可裸确认 | 钉钉回「确认」即后端写入并回结果日志；半码按码数舍去小数 |
| 2026-08-17 | D11 | — → 完成 | 鞋垫写入完成后主动发【任务完成】，不再让员工反复查询 |
| 2026-08-17 | C4 | 部分 95（补镜像回写） | 写入成功后按单刷新/回写镜像，定位排除 48 小时内已写入单，避免钉钉重复查出同一批 |
| 2026-08-17 | C4 / D4 | 钉钉清单只展开 5 条 | 待处理/结果只保留单号、状态、鞋码和目标鞋垫，避免一条消息刷屏 |
