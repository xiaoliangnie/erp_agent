# Agent 系统完成度（已并入 `docs/开发.md` §16）

> **归档。** 以后改完成度请改 [`../开发.md`](../开发.md) 第 16 节，不要再改本文。

| 项 | 值 |
|---|---|
| 统计日 | 2026-08-18 |
| **总进度** | **88%** |
| 算法 | 计入「完成 / 部分 / 骨架 / 未做」；「不做」不进分母。条目等权，再按分组加权（见下） |

**怎么改进度**

1. 在对应分组表改该行的状态和 `%`。
2. 重算该分组小计（组内条目等权平均，四舍五入到整数）。
3. 按文首权重重算总进度。
4. 在「最近变更」加一行：日期、条目、从 → 到、原因。
5. 新增能力先加一行再标状态，不要只改总进度。

状态：`完成` 已闭环可用 · `部分` 主路径能用但仍有缺口 · `骨架` 代码在、默认关或缺数据 · `未做` · `不做` 本期不做（不扣分）

---

## 总览

| 分组 | 权重 | 小计 | 一句话 |
|---|---:|---:|---|
| A 内核（注册表 / 确认 / 会话 / 双通道） | 20% | 99% | 对话内核可用；上下文按层组装；黄金五类原话可路由；网页要钉钉身份码 |
| B 只读查询工具 | 20% | 100% | 采购、订单、国标、缺口都能查 |
| C 写操作与 ERP | 20% | 78% | 合同和鞋垫能确认后写；写入后回读 SKU；换货/图片/探测走后端 Playwright；登录态随服务启停 |
| D 钉钉与意图 | 20% | 91% | 对话、催办、品控、意图路由按设计落地；换鞋垫查询/处理都直分派；员工私聊只确认；绑定需「同意绑定」；催办预览冻结；Outbox 可补发；多群未做 |
| E 身份与权限 | 10% | 77% | users 表 + 镜像库种子 51 人；管理员只认已绑定 userId，extra_ids 不再提权；网页 webToken；权限表未建 |
| F 预测与订货 | 5% | 37% | 接口和工具在，缺销售/库存表和正式模型 |
| G 工作台与后置 | 5% | 97% | `/workbench`、换货 Impact、Job 租约、Outbox 已落地 |
| **合计** | **100%** | **88%** | `0.20×99 + 0.20×100 + 0.20×78 + 0.20×91 + 0.10×77 + 0.05×37 + 0.05×97` |

---

## A 内核

| ID | 条目 | 状态 | % | 代码 / 说明 |
|---|---|---|---:|---|
| A1 | 工具注册表（名称 / Schema / 风险 / handler） | 完成 | 100 | `backend/agent/tools.py` |
| A2 | L0 直接执行 | 完成 | 100 | `runner._invoke` |
| A3 | L1/L2 pending 确认（谁发起谁确认、幂等一次） | 完成 | 100 | `actions.py`；网页和钉钉「确认 编号」；鞋垫也可只回「确认」；同时只一批 `confirmed`；重启后退回 pending 并私聊补发，5 分钟未确认释放 |
| A4 | 运行 / 工具 / 确认审计 | 完成 | 100 | `audit.py`；过长 JSON 改成合法 `truncated` 对象，不截半截字符串 |
| A5 | 会话隔离、同会话串行、空闲翻篇 | 完成 | 100 | `sessions.py` |
| A6 | 网页 `/chat` | 部分 | 94 | 群里「绑定网页」私信 20 位码，网页姓名+码换 `webToken`；共享 Token 不再代表操作员 |
| A7 | CLI 调试（不经 HTTP） | 完成 | 100 | `scripts/run_agent_cli.py`；`--status` 含品控/代发/Job/Outbox/绑定 |
| A8 | 黄金对话回放 | 部分 | 96 | 已落地场景按五类原话补齐；同话题唯一编号可补槽，多个编号追问时列出候选 |
| A9 | 工具结果信封（摘要进模型、全量给页面） | 完成 | 100 | L0 `ok/summary/data/truncated/hint` |
| A10 | 上下文预算 / 会话摘要 | 部分 | 95 | 组装顺序、业务对象快照、摘要消毒已做；摘要默认关。当前短闭环场景无收益 |

**A 小计 99%**（10 条等权：985/10）

---

## B 只读查询工具

| ID | 条目 | 状态 | % | 工具 |
|---|---|---|---:|---|
| B1 | 搜采购单 | 完成 | 100 | `search_purchase_orders` |
| B2 | 采购单头+明细 | 完成 | 100 | `get_purchase_order` |
| B3 | 跟单三档催办清单 | 完成 | 100 | `delivery_reminders`；与交期台账 / 钉钉同一口径 |
| B4 | 看板汇总 | 完成 | 100 | `dashboard_summary` |
| B5 | 商品主数据搜索 | 完成 | 100 | `search_products` |
| B6 | 国标目录状态 | 完成 | 100 | `gb_catalog_status` |
| B7 | 执行标准查询 | 完成 | 100 | `lookup_gb_standards` |
| B8 | 主数据缺口 | 完成 | 100 | `master_data_gaps` |
| B9 | 销售订单定位（o_id） | 完成 | 100 | `search_sales_orders` |
| B10 | 销售订单明细 | 完成 | 100 | `get_sales_order_items` |

**B 小计 100%**

---

## C 写操作与 ERP

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| C1 | 生成采购合同（确认后出 Excel/预览） | 部分 | 95 | `generate_purchase_contract`；soffice 120s / pdftoppm 30s / outputs 30 天；本机缺套件时预览失败 |
| C2 | 普通换货 dry-run 登记 | 部分 | 90 | `submit_exchange_dry_run`；确认只登记试算 |
| C3 | 换货页二次确认 + 后端 Playwright 写入 | 部分 | 90 | `/exchange` 确认后由 `DigitalWorkerLoop` 执行；油猴领取口已关闭 |
| C4 | 抖音换鞋垫：定位 → 清单 → 确认 → 写入 | 部分 | 97 | 处理不再经 LLM；钉钉回「确认」由后端写；写完发【任务完成】；半码舍去小数；查询池含抖音/快手/视频号；确认后不再重扫全库；同页最多 3 路 ChangeItem；写后回写镜像；待确认只对他人隐藏；重启把未写完写入退回 pending；Delivering 不处理 |
| C5 | 普通换货从钉钉直接写入（不经换货页） | 未做 | 0 | 仍要 `/exchange` 二次确认 |
| C6 | Digital Worker 领取换货队列 | 部分 | 92 | 换货 / 探测 / 搜 SKU / 商品图片都由后端领取；登录态随 `server.py` 启停；`ERP_AI_ENABLED` 默认开。本机 `ping` 已通。鞋垫确认走同一把锁 |
| C7 | 写入回读 / 预览不一致则停 | 部分 | 80 | `loadOrder` 回读 SKU；对不上不记成功；已换过则跳过；JSON 证据落盘。真机联调和截图/trace 仍待本机账号 |
| C8 | 备注异常 / 超卖 / 改地址 | 不做 | — | 第一期只做 SKU 换货 |
| C9 | 正式创建采购单 | 不做 | — | 本期最多草稿预览，未开闸 |

**C 小计 78%**（C1–C7，不含不做：544/7）

---

## D 钉钉与意图

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| D1 | Stream 长连对话 | 部分 | 94 | `DINGTALK_ENABLED`；需 `dingtalk-stream`；员工私聊只确认，管理员私聊全开；@机器人回自我介绍+用法；群里「绑定网页」 |
| D2 | 绑定采购员姓名 | 完成 | 100 | 群内申请；管理员回「同意绑定 / 确认绑定」；有待办时裸「确认」先执行待办；姓名占用不覆盖；双人审批先到先得。群里「绑定成功」@ 申请人 |
| D3 | 「确认 / 取消」 | 完成 | 100 | 可带编号，也可只回「确认」执行当前会话最近一条 |
| D4 | 抖音换鞋垫固定意图 | 完成 | 100 | 查询和处理都直分派；已有待确认再处理会沿用清单；「换鞋垫的动作 / 粘贴单号」可识别 |
| D5 | 每日交期催办推送 | 部分 | 98 | 跟单三档；按钉钉身份拆开只私聊；清单含采购量/待入库；Agent 催办预览冻结订单与收件人 |
| D6 | 品控登记 / 查询 / 日报 | 部分 | 96 | 正则直登 + 可选日报；网页/对话可 L1 关闭/撤销 |
| D7 | 通用意图识别（多业务） | 部分 | 97 | 命令→抽槽→已注册工具/拒答/追问→Agent；当前话题唯一编号可补槽，多个编号列出候选；意图不查权限 |
| D8 | 多群路由（协同群 / 异常群） | 未做 | 0 | S4 |
| D9 | 出站投递台账（失败可补发） | 完成 | 100 | `outbox_messages` + JobWorker 补发；催办走 Outbox |
| D10 | 钉钉卡片轻确认 | 未做 | 0 | 现在是纯文本「确认」，不做 ActionCard |
| D11 | 鞋垫写入完成通知 | 完成 | 100 | 确认后先回「已开始」；写完 `reply_text` 发【任务完成】（含用时）并 @ 发起人 |

**D 小计 91%**

---

## E 身份与权限

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| E1 | `staff_bindings`（姓名 ↔ 钉钉 userId） | 部分 | 88 | 兼容层；可回填 `users.user_id`；网页码绑定同一行 |
| E2 | Web / 钉钉同一 `user_id` | 部分 | 95 | 有 users 则 `usr_`；否则回退钉钉 id / `staff:` |
| E3 | `viewer` / `operator` / `admin` | 部分 | 96 | viewer 不能确认写操作；管理员只认已绑定 userId；`extra_ids` 不再提权；韩立与其他管理员权限相同，只有韩立能改角色。权限表后置 |
| E4 | 用户权限表 / capabilities | 骨架 | 15 | `permissions.py` 已留钩子；表未建。钉钉写操作先要求绑定 |
| E5 | 独立 `users` 表 | 部分 | 85 | schema / 归一化 / 镜像库种子 51 人 / resolve；钉钉通讯录未对齐 |
| E6 | 店铺权限矩阵 | 不做 | — | 本期不做 |
| E7 | 操作员长期记忆 | 部分 | 80 | 「记住 / 忘记」按 `user_id`；注入扫描；仅绑定员工；默认关 |

**E 小计 77%**（E1–E5、E7：459/6）

---

## F 预测与订货

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| F1 | `Forecaster` 接口 + Baseline | 完成 | 100 | `backend/forecast/` |
| F2 | `forecast_demand` 工具 | 部分 | 80 | 已注册；无工件则不可用 |
| F3 | `order_suggestion` 工具 | 部分 | 80 | 库存缺失直接报错，不用 0 兜底 |
| F4 | 销售出库表进镜像 | 未做 | 0 | `FORECAST_SALES_*` 仅配置 |
| F5 | 现势库存表进镜像 | 未做 | 0 | `FORECAST_INVENTORY_*` 仅配置 |
| F6 | 拆出 `forecast_sales` + `forecast_id` | 未做 | 0 | F3，Gate A 之后 |
| F7 | 业务可用的训练工件 | 骨架 | 20 | 可 `--csv` 训练；生产模型未接入 |

**F 小计 37%**

---

## G 工作台与后置（不挡主线）

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| G1 | `/workbench` 待办列表 | 完成 | 100 | `/workbench` + `GET /api/agent/workbench`；确认/取消复用 pending_actions；品控关闭/撤销走 `/api/agent/quality/{id}/resolve` 或 `cancel` |
| G2 | 换货 Impact Analysis | 完成 | 100 | `backend/exchange/impact.py`；重复任务 / 目标 SKU 不存在 / 跨店 |
| G3 | 长任务 Job 队列 | 部分 | 88 | `jobs` + 进程内 worker；`running` 300 秒租约过期回收；合同预览仍在确认线程 |
| G4 | 钉钉 Outbox | 完成 | 100 | 与 D9 同一实现 |
| G5 | `supplier_scorecard` | 不做 | — | `RESERVED_TOOLS` 占位 |
| G6 | 代发只读（模板 + Digital Worker 抓取） | 部分 | 92 | `YYMMDD-代发.xlsx` 对齐 8.15；13:30 备表、14:00 发群并私聊安安；已填表不覆盖 |
| G6b | 采购草稿 | 不做 | — | S4 |
| G6c | 知识库 | 不做 | — | S4 |

**G 小计 97%**（G1–G4 等权；G6 不进本组底）

---

## 最近变更

| 日期 | 条目 | 从 → 到 | 原因 |
|---|---|---|---|
| 2026-08-17 | 本文 | — → 首版 | 建立 Agent 完成度进度表 |
| 2026-08-17 | C4 / D4 / E4 | 未做 → 部分/骨架 | 钉钉换鞋垫：定位清单 + 二次确认 + 串行写入；意图与权限钩子落地 |
| 2026-08-17 | C4 / D3 / D4 | 部分 90 → 95；D3 可裸确认 | 钉钉回「确认」即后端写入并回结果日志；半码按码数舍去小数 |
| 2026-08-17 | D11 | — → 完成 | 鞋垫写入完成后主动发【任务完成】，不再让员工反复查询 |
| 2026-08-17 | C4 | 部分 95（补镜像回写） | 写入成功后按单刷新/回写镜像，定位排除 48 小时内已写入单，避免钉钉重复查出同一批 |
| 2026-08-17 | C4 / D4 | 钉钉清单只展开 5 条 | 待处理/结果只保留单号、状态、鞋码和目标鞋垫，避免一条消息刷屏 |
| 2026-08-17 | D11 | 结果日志加用时 | 【任务完成】带上处理耗时（秒 / 分秒） |
| 2026-08-17 | G6 | 不做 → 骨架 | 代发空白模板落地；抓取走同一套 DigitalRuntime，与采购草稿/知识库拆开 |
| 2026-08-17 | G6 | 骨架 → 部分 55 | 恢复 8.15 全列（隐藏列同样例）；SKU 行写入裸价，供应商/成本价补自商品主数据 |
| 2026-08-17 | G6 | 部分 55 | 模板按 8.15 刷新且不再覆盖当日已填表；缺供应商的 SKU 走页面 GetSku |
| 2026-08-17 | C4 / A3 | 鞋垫写入互斥 | 待确认/写入中的单号不再发给其他人；同时只允许一批 `process_insole_orders` 写入，后来的确认排队 |
| 2026-08-17 | C4 / D4 | 查询池+写入加速 | 快手/视频号与抖音同一策略；确认不再重扫全库、跳过按尺码试算，写入间隔 50ms |
| 2026-08-17 | C4 | 写入并发 | 鞋垫 ChangeItem 同页最多 3 路；核心脚本 0.7.0 版本不符会重注入 |
| 2026-08-17 | C4 / C6 | 登录态保活 | 【任务完成】拆开页/写入；北京时间 09:30–18:30 保活 ERP 登录态，不固定订单页 |
| 2026-08-17 | D5 | 跟单催办 | 每日推送改为已确认未完结、排除返修、10/3/逾期；谁的单只通知谁 |
| 2026-08-17 | D5 | 催办 @ | 应用机器人主动群发不支持 @；改为 sessionWebhook / 自定义 Webhook 的 text + at |
| 2026-08-17 | D5 | 催办私聊 | 已绑定员工改走 `oToMessages/batchSend` 单聊，早上催办不再依赖群 @ |
| 2026-08-17 | B3 | 四波 → 跟单三档 | 交期台账页、Agent 工具与钉钉催办对齐：已确认/排除返修/10·3·逾期 |
| 2026-08-17 | G6 | 部分 55 → 60 | 代导出封装为 L1 工具 `generate_dropship_workbook`，确认后才抓 ERP |
| 2026-08-17 | G1–G4 / D9 | 未做 → 完成/部分 | S2 Gate B：工作台、换货 Impact、Job、Outbox、预览冻结；镜像扩源不做 |
| 2026-08-17 | A10 / E7 | 80/骨架 → 95/部分 | S5：上下文分层、业务对象快照、摘要消毒、记忆扫描与 user_id、忙时回复 |
| 2026-08-17 | D7 / A8 | 骨架/70 → 80 | 意图路由：黄金原话抽槽后直接调工具，缺参追问、越权拒答 |
| 2026-08-17 | C7 | 骨架 40 → 部分 80 | S3：写入前快照、回读 SKU、不一致不记成功、已换过跳过；JSON 证据落 `erp-evidence` |
| 2026-08-17 | C6 | 骨架 45 → 部分 70 | 本机 `ping` 已通订单页；领取队列仍默认关 |
| 2026-08-17 | C3 / C6 | 80/70 → 90/90 | 油猴退役；换货、探测、搜 SKU、商品图片一律走后端 Playwright |
| 2026-08-17 | E5 / E1 / E2 | 未做/部分 → 部分 80/85/95 | users 表、采购员归一化、staff_bindings 兼容；三三与鞋子理单合并 |
| 2026-08-17 | D7 | 80 → 85 | Intent Router 骨架接入 runner；「604264 到货了吗」走 exact_query |
| 2026-08-17 | E5 | 80 → 85 | 镜像库只读扫描 81 署名，自动种子 51 人；待确认 13 条未建用户 |
| 2026-08-17 | E5 | 人工确认韩立=管理员 | 冲突别名只记录，不自动合并 |
| 2026-08-17 | A8 / D7 | 80/85 → 85/88 | 合同缺票种追问；代发/鞋垫黄金原话；payload 有序列名 |
| 2026-08-18 | A8 / D7 / C1 | 85/88/90 → 88/90/95 | 明确 o_id 状态+明细两步 L0；合同预览超时收口记入进度 |
| 2026-08-18 | A8 / D7 | 88/90 → 95/96 | 意图按设计补齐：已注册工具可路由，建单/询价拒答，黄金五类原话，Intent 与权限分开 |
| 2026-08-18 | A8 / D6 / D7 / G6 | 95/90/96/60 → 96/96/97/70 | 话题唯一编号补槽；品控关闭/撤销 L1；代发 14:00 定时默认关 |
| 2026-08-18 | G1 / A7 / D7 | 说明更新 | 工作台可关品控；CLI/status 含调度与 Outbox；多编号追问列出候选 |
| 2026-08-18 | D5 | 95 → 97 | 催办只私聊已绑定；采购量与待入库分列；未绑定不再刷群 |
| 2026-08-18 | C4 / D4 | 95/95 → 96/97 | 利特原话「换鞋垫的动作 / 粘贴单号」进 process；本人待确认不再把自己的单藏掉 |
| 2026-08-18 | A3 / C4 / D4 | 说明 | 鞋垫处理经 LLM 审核后必须调 process；重启把 confirmed 退回 pending，清单不丢 |
| 2026-08-18 | A3 | 说明 | 重启后未确认任务再私聊提醒；5 分钟未确认释放占用 |
| 2026-08-18 | D1 / D2 / E3 | 90/100/80 → 93/100/88 | 员工私聊只收催办和确认；绑定改管理员审批，可批量；管理员私聊全权限 |
| 2026-08-18 | D1 | 说明 | @机器人 /「帮助」改为自我介绍 + 功能用法 |
| 2026-08-18 | D4 | 说明 | DeepSeek 思考模式拒 `tool_choice=required` 时改 auto 重试，避免换鞋垫处理直接失败 |
| 2026-08-18 | C4 / D4 | 96/97 → 97/100 | 换鞋垫处理改回固定分派，不经 LLM；再发「进行处理」沿用已有待确认 |
| 2026-08-18 | G6 | 70 → 88 | 代发定时把当日已填表发钉钉群，不覆盖、不重写收货明文 |
| 2026-08-18 | D2 | 说明 | 绑定审批改回「确认」；同意后群里 @ 申请人报绑定成功 |
| 2026-08-18 | G6 | 88 → 90 | 代发定时除群文件外，再私聊一份给安安 |
| 2026-08-18 | C6 | 90 → 92 | Playwright 登录态随服务启停，不再按 09:30–18:30 关浏览器 |
| 2026-08-18 | G6 | 90 → 92 | 代发 13:30 抓表，14:00 只发送，避免到点才开 ERP |
| 2026-08-18 | A6 / D1 / D2 / E1 / E3 | 90/93/100/85/88 → 94/94/100/88/94 | 管理员不再认显示名；网页用钉钉 20 位码换 webToken；看板/合同鉴权仍后置 |
| 2026-08-18 | D2 / D5 / E3 / G3 / A4 | 说明 + D 90→91 / E 76→77 / G 95→97 | extra_ids 不再提权；绑定改「同意绑定」并处理确认冲突；催办预览冻结；Job 租约；审计不再截半截 JSON；摘要默认关 |
