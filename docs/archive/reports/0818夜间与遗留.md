# 0817 夜间工作与 0818 遗留

> 统计日：2026-08-18。覆盖 8 月 17 日夜间到 18 日凌晨的离线推进。
> 执行顺序仍以 [`../开发.md`](../开发.md) 为准；完成度看 [`../Agent进度.md`](../Agent进度.md)（约 **87%**）。
> 本文只回答两件事：**昨晚做了什么**，**还剩什么要拍板 / 真机 / 后置**。

## 一句话

身份、意图、工作台、代发定时这些能离线落地的都做了。2026-08-18 白天又收口了管理员模型、绑定确认冲突、催办预览冻结、Job 租约、审计 JSON 和 health 探活。通讯录 / 手机号 / 冲突花名入表 / 预测镜像 **已拍板不做或后置**。白天真机只剩换鞋垫 E2E 和看板看一眼。S4 / Gate D 仍后置。

---

## 1. 昨晚做了什么

按主题，不按提交。都是小步演进，没有重写框架，没有改 MySQL 镜像表结构。

### 1.1 身份（`0817开发.md` P0，Gate A 代码侧）

- Agent SQLite 建 `users`（`usr_` + 10 hex），`staff_bindings` 兼容回填 `user_id`，不删旧表。
- 采购员署名 NFKC 归一；`scripts/seed_users.py` 只读扫镜像。
- 镜像库 81 个署名称 64 簇：自动种子 **51** 人；冲突别名只记不建用户。
- 已确认：韩立 = ERP「管理员」；利特 → 李佳冬；三三 = 鞋子理单（三三）。
- Web / 钉钉有 `users` 命中则同一 `usr_`；未命中仍回退署名 / 绑定。
- **不**在 resolve 或钉钉绑定时静默建用户。

报告：[`user_identity_analysis.md`](user_identity_analysis.md)。规则：[`../identity/用户身份.md`](../identity/用户身份.md)、[`../adr/0001-user-identity.md`](../adr/0001-user-identity.md)。

### 1.2 意图路由（P1，做到设计口径）

`route_message`：会话命令 → 抽槽/关键词 → 拒答 / 追问 / 已注册工具 → 才进 LLM。**不查权限。**

已覆盖：采购单、销售订单、跟单、看板、商品、国标、缺口、预测/订货建议（必须带 SKU，不改分位键名）、合同、代发、换货、鞋垫、品控登记/列表/推送/关闭/撤销。

拒答：正式建单、询价/RFQ、历史报价、改地址、超卖。相邻意图（到货+合同、逾期+鞋垫、代发+合同、缺价+合同）先追问。

黄金原话在 `tests/fixtures/golden_dialogues.json`（正常 / 省略 / 模糊 / 错误 / 相邻干扰）。

### 1.3 话题编号（工作集）

- 当前 epoch 里**唯一**采购单 / `o_id` / SKU 可补槽（「里面有哪些 SKU」不必重说单号）。
- **两个及以上**不猜；追问会列出候选：「当前话题有销售订单 11530151、11530152，请指明要看哪一个。」
- Runner 在写入本轮用户消息之前抽快照，避免把刚说的编号当成「已经在话题里」。

### 1.4 品控

- 对话：`resolve_quality_issue` / `cancel_quality_issue` 为 L1，走现有 `QualityLedger`，批量关闭先追问。
- 工作台：开放中的品控卡有「关闭 / 撤销」。`POST /api/agent/quality/{issueId}/resolve|cancel`。
- 校验：未绑定拒绝、viewer 拒绝、只能处理自己登记的（花名互认）。不另开第二本台账。

### 1.5 代发

- `DailyDropshipScheduler`：到点保证当日 `YYMMDD-代发.xlsx` 在（已填表不覆盖），再发钉钉群文件。
- 群消息只写行数和文件名，收货明文只在 xlsx 里。
- 空则复用 `DINGTALK_GROUP_CONVERSATION_ID`。health JSON 带 `dropship`。

### 1.6 现网收口与可观测

S0 对照代码后标成已做（本来就在）：soffice/pdftoppm 超时、outputs 30 天清理、CSV 公式注入加引号、`business_today()`、税率/单价 fail-fast、图片下载禁止跟到内网（含重定向）。

payload：`orders` / `lines` 带有序 `columns`；前端按列名解码，列名不一致直接报错。

状态补齐：

- `GET /api/agent/status`、`run_agent_cli.py --status`、`run_dingtalk_cli.py status`：品控 / 代发 / Job / Outbox / 绑定人数
- `health_watch`：品控、代发的 `lastError` 与「已启用但线程没在跑」

### 1.7 调研与接口预留（P3 / P4，没有做成产品）

- [`../research/`](../research/)：Grok Build、DeepSeek Harness、Memory、Runtime 建议
- [`../adr/0002-rag-and-erp-direction.md`](../adr/0002-rag-and-erp-direction.md)
- `MemoryStore` / `RetrievalRouter` 空壳；工具声明 `domain` / `concurrency_mode`
- **没有**接向量库、没有开摘要/记忆默认值

### 1.8 明确没做（避免白天重复）

采购草稿 / `create_purchase_draft` 已取消，不要复活。  
`forecast_sales` 等 F0 分位冻结后再注册。  
合同预览仍在确认线程（员工要立刻拿到文件），不迁 Job。  
没有按供应商拆代发表、没有扩 Memory/Retrieval。

---

## 2. 遗留问题

### 2.1 要拍板（2026-08-18 已回）

| # | 事项 | 结论 |
|---|---|---|
| 1 | 预测分位 F0 | **代办**：预测工具完成后再考虑，现接口继续 `p10/p50/p90` |
| 2 | 钉钉通讯录范围 | **不做**通讯录同步 |
| 3 | 手机号预绑定 | **不做**手机号反查 userid |
| 4 | 冲突花名 | **不入 `users`**，留在 `DEFERRED_ALIAS_REVIEWS` |
| 5 | 镜像扩源 | **代办**：与预测工具一起后再做 |

同日已落地、不再问：

- 看板/合同鉴权后置；服务将收纳不暴露公网。Excel 仍写完整银行账号，options 本轮不裁剪
- `DINGTALK_ADMIN_USER_IDS` 不再提权；韩立与其他管理员权限相同，只有韩立能设/取消管理员
- 绑定审批改「同意绑定 / 确认绑定」；有待办时裸「确认」先执行待办；姓名占用不覆盖
- E4 权限表后置（仍测试）
- 催办预览冻结、Job `running` 租约、审计不截半截 JSON、health 用 `SELECT 1`
- 会话摘要默认关：当前短闭环场景没有收益

冲突花名（`backend/agent/users.py` 的 `DEFERRED_ALIAS_REVIEWS`）：

| 署名 | 可能相关 | 原因 |
|---|---|---|
| 黄娟25 | 黄娟 | 数字后缀 |
| 戴启伟17 | 戴启伟 | 数字后缀 |
| 熊凯丽1&10 | 熊凯丽 | 数字后缀 |
| 魏大可（01） | — | 括号内工号 |
| 吴慧（03） | — | 括号内工号 |
| 小溪-商品实习生 | — | 岗位/临时 |
| 临时-张龙祥、临时工-夏梓轩、临时工-涂猛、临时工-赵祥兵 | — | 临时工 |
| 杭州仓-利特 | 李佳冬/利特 | 仓前缀，未并入利特 |
| 无际云帆-协助机、邹灵念11 | — | 不像人名 |

### 2.2 要真机（代码在，没走通不算过门）

| # | 事项 | 怎么验收 | 备注 |
|---|---|---|---|
| 1 | Gate C 换鞋垫 E2E | 候选 `o_id` → Impact → 预览 → 确认 → 后端写 → `loadOrder` 回读 | 正式建单、其他出库仍关 |
| 2 | Digital Worker 本机 | `scripts/run_erp_worker.py status` / `ping` | 本机账号 |
| 3 | 钉钉身份对齐 | 利特、韩立绑定已回填。通讯录 / 手机号反查已拍板不做 | 继续手绑；不要为 Web / 钉钉 / ERP 建三套人员表 |
| 4 | Gate A 双通道一眼 | 同一人 Web 与钉钉 `user_id` 一致 | 代码路径已通，缺真机点一下 |
| 5 | 看板 / 台账 | 浏览器看 `/dashboard` `/ledger` | payload 已带 `columns`；旧前端缓存无列名时仍按宽度解码 |

### 2.3 本机环境（可顺手，不挡业务）

- `tests.test_dingtalk.StreamSslTests` 在系统 Python 上缺 **certifi** 会失败。`requirements.txt` 已有；用仓库 `.venv`，或给系统 Python 装 certifi。
- 合同预览本机缺 soffice 会失败，是有意的。

### 2.4 明确后置（本期不要做）

**S4（Gate B/C 之后单独开闸）**

- 按供应商拆代发表
- 采购草稿预览
- 多群路由（D8：协同群 / 异常群）
- 准交率、知识库

**仍关掉 / 未做**

- C5 普通换货从钉钉直写（仍走 `/exchange`）
- D10 ActionCard
- E4 用户权限表 / 店铺矩阵（E6 不做；权限表测试阶段后再设）
- F4/F5 镜像扩表、F6 `forecast_sales`（预测工具完成后再做）
- 摘要 / 记忆默认关（`AGENT_SUMMARY_ENABLED` / `AGENT_MEMORY_ENABLED`）；当前业务场景开摘要无收益
- 代发按供应商拆表未做
- 钉钉通讯录、手机号反查、冲突花名入 `users`

**Gate D / 方案里写明不做**

SSO / OAuth / 完整 RBAC、向量库、Neo4j、通用 Browser Agent、自主 Skill、RFQ、正式建单、邮件门户、多 Agent、财务利润。

---

## 3. 白天建议顺序

一次只推一类，不要并行开 S4。

1. **ERP 换鞋垫走通**（真机 1、2）：过 Gate C；不要趁机开正式建单。
2. **看板看一眼**（真机 5）。
3. 预测工具完成后再回头看 **F0 分位 / 镜像扩源**；通讯录、手机号、冲突花名入表不要做。

---

## 4. 和别的文档怎么分工

| 文档 | 看什么 |
|---|---|
| **本文** | 昨晚交付 + 现在还能做什么 / 不能做什么 |
| [`../开发.md`](../开发.md) | 阶段、已拍板、过门条件 |
| [`../Agent进度.md`](../Agent进度.md) | 功能百分比 |
| [`0817后置事项.md`](0817后置事项.md) | 17 日当晚的短清单，内容已并入本文 |
| [`../0817开发.md`](../0817开发.md) | 17 日任务书原文，不再当执行清单改 |

未提交。凭证、供应商 Excel、`files/data/` 运行库不要进版本库。
