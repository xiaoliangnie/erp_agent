# 0817 后置事项（已并入 0818 纪要）

> 完整清单改看 [`0818夜间与遗留.md`](0818夜间与遗留.md)。本文保留 17 日当晚的短记录，不再追加。

今晚按文档继续做了能离线落地的部分。下面这些需要你拍板、钉钉真机、ERP 真机或本机环境，**不要在未确认前改镜像表结构**。

## 今晚已完成（不用再做）

- P0–P4：`users`、采购员归一化、兼容 `user_id`、Intent Router 骨架、调研/ADR/接口预留
- 镜像库只读扫描 + `--seed`：51 人；韩立 = ERP「管理员」
- 冲突别名只记录，不合并
- S0 payload 有序列名：响应带 `columns`，前端按列名校验/解码
- 合同缺票种追问；代发 / 鞋垫黄金原话
- 明确 `o_id` 且同时问状态+明细：连续两个 L0，不进模型
- 意图识别按设计补齐：已注册工具可路由；建单/询价/报价拒答；相邻意图追问；黄金五类原话；`route_message` 不查权限
- 当前话题唯一编号可补槽（「里面有哪些 SKU」不再必须重说单号）
- 品控关闭/撤销走 L1 工具，接现有台账
- 代发 14:00 定时已落地，默认关，不覆盖已填表；钉钉发文件仍后置
- S0 里合同校验、soffice 超时、outputs 清理、CSV 公式注入、`business_today()` 本来就已经在代码里，执行文档已改成「已做」
- 工作台可关闭/撤销自己登记的品控（viewer / 未绑定 / 别人的单拒绝）
- 话题里有多个单号时追问会列出候选，仍不猜
- CLI `--status`、`/api/agent/status`、health_watch 补上品控 / 代发 / Job / Outbox

## 1. 钉钉员工对齐（真机 / 权限）

**目标：** `DingTalk userId → users.dingtalk_userid → user_id → ERP aliases`。

利特、韩立绑定已回填。明天若核对通讯录，请确认：

1. 企业应用是否已开通通讯录只读（部门、userid、unionid、mobile）？
2. 是否允许用手机号反查 userid 做预绑定？

不要为 Web / 钉钉 / ERP 建三套人员表。

## 2. ERP / Digital Worker（真机）

Gate C 过门要走：候选 `o_id` → Impact → 预览 → 确认 → 后端执行 → 回读。

今晚**没有**测换货/建单。明天若顺手：`scripts/run_erp_worker.py status` / `ping`。

正式建单、其他出库仍关闭。

## 3. 冲突别名 — 先不管（已记录）

见 `backend/agent/users.py` 的 `DEFERRED_ALIAS_REVIEWS`。重新跑 seed 不会建成 User。

黄娟25、戴启伟17、熊凯丽1&10、魏大可（01）、吴慧（03）、临时工、小溪-商品实习生、杭州仓-利特、无际云帆-协助机、邹灵念11。

## 4. 预测口径（要你拍板）

F0：现接口是 `p10/p50/p90`，方案写过 `P50/P80/P90`。改调用方前先定一种。今晚不改。

销售出库 / 现势库存进镜像属于镜像扩源，等单独拍板。

## 5. 本机环境（可顺手）

- `tests.test_dingtalk.StreamSslTests` 在系统 Python 上因缺少 **certifi** 失败，与本轮改动无关。装 certifi 或用仓库 `.venv` 即可。
- payload 改了契约后，看板/台账请在浏览器看一眼（旧前端缓存无 `columns` 仍能按宽度解码）。

## 6. 下一阶段仍后置

S4：代发钉钉发文件、采购草稿、多群路由。代发定时已做（默认关）。  
Gate D：RAG / RFQ / 正式建单。  
SSO / OAuth / 完整 RBAC / 向量库 / Neo4j / 通用 Browser Agent / 自主 Skill / 镜像库新增表。
