# docs 目录索引

> 2026-08-13 整理。仓库根的 `README.md` 是**全部业务口径的唯一权威**，`CLAUDE.md` /
> `AGENTS.md` 是协作与提交约定；本目录放设计、方案与外部接口参考。

## 阅读顺序（新人接手）

1. 仓库根 `README.md` —— 业务口径（四波催办、票种、日期口径……）
2. `PROJECT_DESIGN.md` —— 第一阶段数据链路怎么搭起来的
3. `AGENT_ARCHITECTURE.md` —— Agent 总架构、风险分级、明确不做的事
4. `完善方案v3-整改与Agent演进.md` —— **当前执行文档**（审查整改 · 品控台账 · 记忆与会话）
5. `会话管理与钉钉机器人方案.md` —— Hermes 调研、多群路由、上下文/记忆规则及 ERP 异常换单分类

## 文档清单

| 文档 | 定位 | 状态 |
|---|---|---|
| `PROJECT_DESIGN.md` | 第一阶段数据链路设计（镜像库 → payload → 页面） | 现行，与实现一致 |
| `AGENT_ARCHITECTURE.md` | Agent 架构方案：工具注册表、确认状态机、通道层、风险分级、实施路线 | 现行；§10 与实现的两处差异已在文内声明 |
| `会话管理与钉钉机器人方案.md` | Hermes 会话/上下文/记忆调研、单机器人多群方案、ERP 异常及换单分类 | 现行；2026-08-14 已用 ERP 实页和本地镜像复核 |
| `完善方案v3-整改与Agent演进.md` | 2026-08-13 全量审查后的整改路线 + 品控台账设计 + 上下文/记忆/会话方案 + 本目录归纳 | **当前执行文档** |
| `MVP完善方案.md` | v2 整改方案（2026-08-13 制定，P0 与 P1-5~8 已落地） | **归档**：未完项由 v3 §1.5 接管，不再往里加内容 |
| `预测模型接入.md` | 第三方 Forecaster 接入指南（唯一耦合点 `metadata.json` 的 `forecaster`） | 现行，与代码一致 |
| `商品资料与图片入库.md` | 商品主数据接入现状 + 未来小图二进制入库的设计参考 | 现行（前半是现状，后半是参考） |
| `聚水潭数据接口记录.md` | ERP 上游接口参考（`#_jt_data` 模式、字段清单，含品控可用的 `qc_*` 质检字段） | 外部参考 |
| `供应链项目-API调用说明(1).md` | 供应链安全代理 API 的调用说明（Client ID、通用请求头、边界） | 外部参考；文件名的"(1)"是下载痕迹，可安全改名（无引用） |
| `assets/` | 截图素材（实时库字段等） | 素材 |

## 待处置

- ⚠️ `api.txt`：一行疑似 Client Secret 的明文凭证，该值不在 `.env` / `hanli.env` 中，
  全仓无任何引用。**已作废请删除；仍有效请移入 `.env` 后删除**——凭证不放 docs
  （见 CLAUDE.md「不要提交凭证」）。处置结论出来前暂保留原样。

## 归档产物去哪

- 审查报告、生成的合同/日报等一次性产物在 `outputs/`（不进版本库）；
  当前最新一份审查存档：`outputs/审查报告-子代理汇总-20260813.md`。
- 模型工件在 `data/models/`（gitignored），接入方式见 `预测模型接入.md`。
