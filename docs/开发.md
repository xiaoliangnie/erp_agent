# 下一阶段开发

> **本文是当前执行文档。** 2026-08-17 从 v3 未完项、V5 评审稿、会话方案和销量预测方案抽出「先做什么」。
> 详细设计仍读原文，这里只写顺序、边界和做到什么算过。
>
> 冲突时：**代码与真实数据 > 本文 > [`架构.md`](架构.md) > `archive/`。**
> 业务数字口径仍以仓库根 `README.md` 为准。

## 覆盖了什么

本文是从 2026-08-17 上传的三份材料抽出的**下一阶段执行清单**，不是 V5 全文任务表。

| 来源 | 进了本文的 | 明确不做 / 后置 |
|---|---|---|
| V5 评审稿 | 薄 Agent、身份、会话 S1/S2、ToolSpec、Work Item/Job/Outbox、Digital Worker、换货闭环 | 多 Agent、RAG/Neo4j、RFQ、正式建单、财务利润、微服务 Studio |
| 预测两个 Word | F0–F4 旁路；与现有 `Forecaster` 对齐 | 独立 Forecast Service、情景推理、深度时序，放到 F 过线之后 |
| 原 `路线.md` R1/R2 | S0 现网收口 | 迁机器才做的项 |
| 0817 任务书 / ADR / 调研 | 身份、Intent、记忆边界、RAG/ERP 方向 | 通讯录、向量库、自主 Skill、完整 IAM |
| 会话方案 / 原 Agent 架构 | 确认、上下文、钉钉入口、换单分类 | Hermes 全文、V5 平台产品 |

V5 十个业务域里，本期只吃「订单异常换货 + 已有合同/催办」。S4（采退、准交率页面、知识库、采购草稿、多群）2026-08-19 先不做。商品计划改「导出 + 公式 Excel」：库存/出库镜像与鞋服 SPU 总表已落地，看板后置。

归档原文的**仍有效结论**已经写进本文和 [`架构.md`](架构.md) / [`预测.md`](预测.md) / [`接口参考.md`](接口参考.md)。`archive/` 只备查过程，不再当执行依据。

## 0. 怎么读

| 要看什么 | 去哪 |
|---|---|
| 接下来做什么、已拍板、完成度（本文） | `docs/开发.md` |
| 现有系统怎么工作 | [`架构.md`](架构.md) |
| 预测（需求、F0 / F0.1、ERP 对照、怎么换模型） | [`预测.md`](预测.md) |
| 预测第一次出数（归档） | [`archive/reports/预测数据与基线分析.md`](archive/reports/预测数据与基线分析.md) |
| 聚水潭 / 供应链代理字段 | [`接口参考.md`](接口参考.md) |
| 旧方案、调研、0817 任务书 | [`archive/`](archive/) |

平台定位（V5，本期按最小版本落地，不按平台产品做）：

> Agent 是自然语言控制面，Web 是结构化工作台，钉钉是协同入口，确定性工作流是执行核心，镜像库是事实来源。

---

## 已拍板（不要翻案）

| 事项 | 结论 |
|---|---|
| 合同数量带小数 | 按 Decimal 原值写入，禁止 `int()` 截断 |
| `DINGTALK_ENABLED` | 总闸：关掉则发送通道不装配 |
| 合同页接口鉴权 | 网页须登录后才能打开面板和看板/合同接口。**Excel 生成仍从本机 `供应商管理.xlsx` 写完整银行账号**；options 接口本轮不裁剪账号 |
| 品控登记 | 正则直登；LLM 抽取走 L1 确认 |
| 品控日报 | 企业机器人真文件进群；webhook-only 降级为摘要 + 签名链接 |
| 预测与采购 | 预测 ≠ 建议量 ≠ 下单；LLM 不得改工具数字；库存缺失不得用 0 兜底。`coverage_days` = `lead_time_days`（不加检查周期）；分位数 `p10/p50/p90`；先销后采扣合格在途；提前采购用待发作预测下界。ERP 只对照。公式见 [`预测.md`](预测.md) §3 F0.1 |
| 实时数字 | 禁止 RAG 查实时 ERP 数字；制度/SOP/历史附件以后可以 RAG |
| ERP 写入 | 模型不写 ERP、不生成页面脚本；写入必须类型化命令 + 确认 + 回读 |
| 身份 | Web / 钉钉落到同一 `user_id`。管理员只认已绑定且 `role=admin` 的钉钉 userId，不认显示名，**`DINGTALK_ADMIN_USER_IDS` 不再提权**。韩立与其他管理员权限相同，只有韩立能「设置管理员 / 取消管理员」。钉钉绑定同意后会私信网页花名和密码；已绑定的人由管理员发「补发网页账号」补发（也可 `issue-web`）。忘记密码到群里发「绑定网页」重置。会话 30 天。页面不再填 `AGENT_API_TOKEN` / 采购员姓名。共享 Token 只给脚本。E4 权限表后置 |
| 确认 | L1/L2 谁发起谁确认，不按金额自动放行 |
| 钉钉换鞋垫 | 查询和处理都是固定意图，直接分派 `locate_insole_orders` / `process_insole_orders`，不经 LLM。人确认才写 ERP。同一会话已有待确认时，再发「进行处理」沿用原清单，不冲掉。钉钉回「确认」由后端写。先回「已开始」，写完再发【任务完成】（开页/写入/写前快照/写后回读拆开）。半码舍去小数。确认后不再重扫全库。同页最多 3 路 ChangeItem。写前用列表缓存；写后只刷改过的单（ACP 异步最多 5 路）。写后回写镜像并排除已写入单。待确认对其他人不可见、本人仍可见。同时只一批写入。进程重启：未写完的 `confirmed` 退回 `pending`，未确认的再私聊提醒一遍，**5 分钟内不确认则释放占用**。抖音另有 09:30–18:30 每小时定时（工作流，不经确认；【开始执行】和总结只发群，不私聊、不限管理员；「手动跑抖音鞋垫」立刻再跑；ERP 占用则跳过）。权限表后置，先拦未绑定 / viewer |
| 钉钉私聊与绑定 | 员工私聊只收催办和任务确认/取消，不走完整对话。绑定必须管理员同意：员工到**群里**发「绑定 姓名」→ 管理员私信通知 → 回「同意绑定 / 确认绑定」（多条一次全过）。光回「确认」时若有鞋垫/催办待办，先执行待办，不批绑定。姓名已被别人占用则拒绝覆盖。两个管理员同时批，先 `decide` 的生效。拒绝仍用「拒绝绑定」。同意后群里发「绑定成功」并 @ 申请人，同时私信网页花名和密码。管理员私聊可用全部能力。已绑定补发：「补发网页账号」或 CLI `issue-web`。忘记密码再发「绑定网页」。CLI `bind` 仍是运维直写口 |
| 代发取数 | 与换鞋垫共用 `DigitalRuntime`：登录态维护 cookie，Playwright 打开 epaas 嵌订单列表；不走镜像/供应链 API，不走「导出订单」（淘系明文导出被平台禁止） |
| 代发钉钉 | 14:00 抓 ERP「代发订单未安排」写成当日表，抓到后立刻发群并私聊安安；通知写数据截至时间；揭收货被限流时点名单号，其余齐了再写「都齐了」；已填表不覆盖；收货明文只在文件里；Excel「商品裸价」对齐系统「商品裸价金额」 |
| ERP 登录态 | 随 `server.py` 启停：启动后暖机并周期性续 cookie，进程退出关浏览器；不再按 09:30–18:30 关 |
| 订单异常第一期 | 只做 SKU 换货，不做备注/超卖/改地址 |
| 运行形态 | 第一阶段单仓库、SQLite 应用库、单进程；迁 MySQL / 拆进程是专用机器之后 |
| 合同供方 | 只读本机 `files/config/供应商管理.xlsx`，不读镜像供应商表 |
| 单价 | 永远不从商品主数据兜底 |
| 催办确认 | 预览冻结当日清单与收件人，确认后不再重查跟单池 |
| Job | `running` 带 300 秒租约，过期退回 `queued`，避免重启后永久卡住 |
| 审计 JSON | 过长时改成带 `truncated/sha256` 的合法对象，禁止截断半截 JSON |
| `/api/health` | 库探活用 `SELECT 1` + 同步状态，不做明细表 `COUNT(*)` |
| 会话摘要 | 默认关。当前鞋垫/催办/合同/品控都是短闭环，工作集已有单号；摘要帮不上忙。开放长聊以后再评 |
| 冲突花名 | **永久不做**：黄娟25 / 戴启伟17 / 杭州仓-利特 / 临时工等不入 `users`。`DEFERRED_ALIAS_REVIEWS` 只挡自动种子，不再当待办 |
| 准交率（自营百货） | 刃海/静静（含只写「刃海」）；第一张入库单时间 `<` 协议到货日；百货只看首批。口径见根 `README.md`。入库表已建，不上页面 |
| 代发拆表 | **永久不做**：不按供应商拆，维持一张 `YYMMDD-代发.xlsx`，私聊安安 |
| 看板/合同 HTTP 鉴权 | 网页须登录；`/api/health`、钉钉品控签名链接、脚本 Token 仍独立 |
| 普通换货钉钉直写 | **网页入口隐藏，接口保留**；无业务场景前不开发普通换货钉钉直写（C5 不做） |
| 钉钉通讯录 / 手机号 | **永久不做**：不通讯录同步、不手机号反查 userid |
| 预测实现 | 复现现有「系统导出 → 公式 Excel」工作流，做成固定步骤；**不训生产模型、不注册 `forecast_sales`**。F0 / F0.1 四层与「不抄 ERP」仍有效。下一步：销售出库 / 现势库存进镜像，等 Excel 模板后再写公式 |
| S4 业务增量 | 采退、准交率页面、知识库、多群路由 **先不做**；看板建采购单已开 |
| 权限 | 测试阶段不细分权限表。绑定后，催办 / 代发 / 鞋垫只把结果推给对应的人 |

R0（数量截断、preview 入参、缓存键、催办线程、四波口径对齐）已落地。

## 1. 原则

1. **薄 Agent + 确定性工具。** LLM 只理解意图、补齐参数、选工具、组织回答。查库、计算、生成文件、权限、ERP 写入、通知全部走普通代码。
2. **能写成固定步骤的不要做成开放 Agent。** 看板、台账、合同、催办、换货执行走页面或定时任务；对话只覆盖路径不确定的问句。
3. **实时业务数字禁止 RAG。** 库存、交期、付款、订单状态必须 Exact Query。RAG 只留给 SOP / 制度 / 历史资料，且验证后再做。
4. **缺数据就停。** 合同、订货建议、预测都不用 0 或占位信息兜底。
5. **数字忠实。** Agent 不得改、补、猜工具返回的数字。预测 ≠ 采购建议 ≠ 下单。
6. **高风险必须闭环。** 影响分析 → 预览 → 确认/审批 → 执行 → 回读核验。L1/L2 谁发起谁确认，不按金额自动放行。
7. **身份跟员工走。** Web 和钉钉落到同一个 `user_id`。共享 Token 只做传输鉴权，不代表操作员。
8. **不让模型写 ERP、不让模型开浏览器。** Playwright 只做类型化命令的 Provider。
9. **第一阶段继续单仓库、单应用库（SQLite）、单进程多线程。** 迁 MySQL / 拆进程是专用机器之后的事。
10. **增量演进，不重写。** 新能力用 Adapter 接现有 `tools.py` / `pending_actions` / 镜像库。

---

## 2. 现在已经有什么

| 能力 | 代码事实 | 缺口 |
|---|---|---|
| 采购看板 / 交期台账 | 镜像库 → payload → `/dashboard` `/ledger`；台账走跟单池（已确认/排除返修/10·3·逾期）；payload 带 `columns` 有序列名 | 旧缓存无 `columns` 时仍按宽度校验 |
| 采购合同 | `/contract` + `contracts.py` + 本机供应商 Excel + 国标目录；税率/单价覆盖 fail-fast；soffice 120s / pdftoppm 30s；outputs 30 天清理 | 本机已能预览；缺套件时仍失败（有意） |
| 换货 | 网页入口隐藏。鞋垫只在钉钉查询/确认后写入；写后回读 SKU；按单回写镜像。抖音另有 09:30–18:30 每小时定时 | 普通换货无业务场景，不钉钉直写 |
| Agent 对话 | 钉钉 Stream、工具注册表、`pending_actions`、`RequestContext`、同会话串行、分层上下文、业务对象快照、摘要消毒、显式记忆。网页 `/chat` 已隐藏 | 摘要/记忆默认关 |
| 已注册工具 | 查采购/订单、交期、看板、商品、国标、主数据缺口、`forecast_demand`、`order_suggestion`、合同、代发 Excel、换货 dry-run、鞋垫定位/处理、催办、品控 | `forecast_sales` 不做；`forecast_demand` / `order_suggestion` 无库存仍不可当员工结论 |
| 意图路由 | `router.py` + `intents.py`：命令 → 抽槽/关键词 → 拒答/追问/已注册工具 → Agent。已覆盖采购/订单/催办/看板/商品/国标/缺口/预测/合同/代发/换货/鞋垫/品控；建单/询价/报价拒答 | 开放分析仍进模型 |
| 预测骨架 | F0 / F0.1 口径冻结；旁路模块在；`/api/forecast/*` | **下一步**：销售出库 / 现势库存进镜像，按现有 Excel 公式做看板。不训生产模型 |
| 钉钉 | Stream、跟单催办、品控日报、换鞋垫固定意图与定时、Outbox；员工私聊只确认，绑定需管理员审批 | 多群路由不做（S4 先不做） |
| 代发 | `YYMMDD-代发.xlsx` 对齐 8.15；14:00 抓取，发群并私聊安安；裸价写系统金额 | 不按供应商拆表 |
| 身份 | `users` + `staff_bindings`；绑定同意 / 补发 /「绑定网页」私信花名+密码，网页登录 30 天 | 测试阶段不细分权限表；未登录不能进面板 |

页面导航七条：`/dashboard` `/ledger` `/spu` `/baihuo` `/purchase` `/contract` `/status`（工作台含运行状态）。`/chat` `/workbench` `/exchange` 重定向到 `/status`。只在 `frontend/src/routes.ts` 加路径。入库单已进镜像。库存/销售出库已进 60 秒同步。

---

## 3. 本期明确不做

不要并行开这些，即使 V5 里有章节：

- 多 Agent、A2A、自主 Skill、自动长期记忆
- 通用浏览器 Agent、模型生成 Playwright / SQL / Shell
- Neo4j、全公司文件 RAG、ERP 表向量化
- 邮件 / 供应商门户全自动沟通
- 大范围正式采购单创建、完整财务利润系统
- 独立微服务、复杂低代码 Studio
- 深度时序模型（TFT / DeepAR）替换树模型
- 把 `order_suggestion` 改成「模型直接出采购量」
- 生产预测模型、注册 `forecast_sales`
- 钉钉通讯录同步、手机号反查 userid
- 冲突花名（黄娟25 等）写入 `users`
- 代发按供应商拆表
- E4 用户权限表（测试阶段只绑定、按人推送）
- options 裁剪银行账号（等服务收纳）
- 预测分位改名（已冻结为 `p10/p50/p90`）
- S4：多群路由、代发采退、准交率页面、知识库（先不做）

V5 里「Agent 允许创建采购单」**本期只做到草稿预览**，正式写 ERP 要过 Gate C 且单独开闸。

---

## 4. 阶段总览

```text
S0  现网收口          穿插，不挡主线
S1  身份与会话        Gate A，必须先过
S2  工作项与执行内核  Gate B
S5  上下文 / 会话 / 记忆  补完发给模型的投影（不新开 Gate）
S3  ERP Digital Worker Gate C（换鞋垫迁后端）
S4  业务增量          先不做（采退 / 准交率页 / 知识库 / 多群）；看板建采购单已开

F   商品计划看板      销售出库+库存进镜像 → 复现现有公式 Excel
                      不训生产模型、不注册 forecast_sales
```

建议一次只推进一个 Gate。S0 的小项可以塞进任何 PR，但不要为了收口再开一条「重构平台」分支。S1–S3 / S5 已过，下一阶段走 F（Excel 工作流），不开 S4。

```text
S0 ──▶ S1 (Gate A) ──▶ S2 (Gate B) ──▶ S5 ──▶ S3 (Gate C)
                                              │
                                              └── 下一步 F：出库/库存镜像 → Excel 公式看板
```

---

## 5. S0 现网收口

目标：现有六页和 Agent 不带已知伤继续用。不引入新子系统。

| 项 | 做什么 | 出处 |
|---|---|---|
| 合同输入 | **已做**：税率 0–100 且有限；单价覆盖必须有限正数；缺单位、付款方式、需方 fail-fast | 路线 R1 |
| 合同渲染 | **已做**：soffice 120s / pdftoppm 30s、预览信号量；`outputs/generated|agent|quality` 30 天清理 | 路线 R1 |
| 确认流 | **已做**：L1/L2 注册必须带 `preview`；钉钉写操作必须已绑定；`cancel` 与确认同一套校验 | 路线 R1 |
| 安全 | **已做**：图片下载禁止跟到内网（含重定向）；`/api/health` 各子系统单独兜错；health 的 `erpWorker` 只回开关/有无凭证，不回账号、路径、登录选择器 | 路线 R1 |
| 换货键 | **已做**：订单/商品款式列默认 `i_id`；页面 `targetStyle` 取商品 `styleCode` | 路线 R2 |
| payload | **已做**：响应带 `columns` 有序列名；前端列名不一致直接报错，旧缓存无列名时仍按宽度校验 | 路线 R2 |
| 台账导出 | **已做**：CSV 对 `=+@-` 前缀加引号，防止公式注入 | 路线 R2 |
| 预测日期 | **已做**：子系统日期走 `business_today()`；`predict()` 校验 p10≤p50≤p90 且非负 | 路线 R2 |
| 订单候选 | **已做**：`search_sales_orders` 补 status / date / shop + 源 SKU；非明确单号默认待发货 | Agent §6 |
| 工具信封 | **已做**：L0 返回 `ok/summary/data/truncated/hint`；审计仍用 `summarize()` | Agent §5.2、会话 S2 的前半 |

迁机器时再做（不进 S0–S4）：合同预览去本机 LibreOffice 缓存、分类/包装/单位改读 `realtime_products`（**单价仍只认配置或 ERP**）、Agent 库迁 MySQL。

---

## 6. S1 身份与会话（Gate A）

目标：Web / 钉钉 / CLI 共用一个 `user_id`，会话不串线，工具能按人做最小权限判断。

**最小实现，不做登录产品、不做权限 Studio。**

| 项 | 落地 | 不要做成 |
|---|---|---|
| `RequestContext` | **已做**：`user_id` / `trace_id` / `role` / `buyer_names`，并预留 tenant/roles/permissions/data_scope。有 `users` 命中则 Web/钉钉同一 `usr_` | 前端自由填姓名当身份；SSO |
| 统一身份 | **已做**：Agent SQLite `users` 是身份事实；`staff_bindings` 兼容层。`scripts/seed_users.py` 从采购员署名种子 | 完整 IAM / SSO / 权限 Studio |
| 角色 | **已做**：`viewer` / `operator`；viewer 不能 L1/L2，拒绝带结构化 `permission` | 按金额自动放行、复杂 RBAC |
| 数据范围 | **已做**：「我名下」只看绑定采购员署名；空 buyer 仍看全部 | 店铺/分类/财务级矩阵 |
| 会话 S1 | **已做**：epoch、空闲翻篇、显式重置、同会话串行锁、悬空 `tool_calls` 剔除 | 多个机器人分流上下文 |
| 会话 S2 | **已做**：字符预算、工具信封、usage 写入 `agent_runs`。摘要消毒与业务对象快照见 **S5** | 默认仍不开启滚动摘要 |
| ToolSpec | **已做**：注册表补 `permission` / `side_effect` / `channels` | 动态发现 ERP 全量对象 |

过门：

- Web 和钉钉同一人，`user_id` 一致，审计不再只记自由文本姓名
- 同会话并发消息串行，不交叉执行
- 空闲翻篇后「上一张单」不会指到昨天
- 未绑定用户不能做 L1/L2
- 工具权限拒绝时有结构化原因，不靠 prompt 拦

详见 [`架构.md`](架构.md) 会话与钉钉。

---

## 7. S2 工作项与执行内核（Gate B）

目标：待确认、执行中、失败不再只活在聊天记录里。HTTP 不跑长任务。

| 项 | 落地 | 不要做成 |
|---|---|---|
| Work Item | **已做**：`work_items` 投影 `pending_actions` / 换货任务 / 品控待办；确认仍走原状态机 | 复杂 BPM、跨部门 SLA |
| `/workbench` | **已做**：待办并进 `/status`；确认/取消 + 品控关闭/撤销；登录后只显示花名 | 独立聊天工作台 |
| Impact Analysis | **已做**：换货确定性规则（重复任务、目标 SKU 不存在、跨店）；库存未进镜像故不做库存结论 | 通用 DSL、LLM 写影响规则 |
| Job | **已做**：`jobs` + 进程内 `JobWorker`；`running` 300 秒租约，过期回收。合同预览仍在确认线程（员工要立刻拿到文件） | 每请求新线程、Scheduler 直接干重活 |
| Outbox | **已做**：催办先落 `outbox_messages` 再发送，失败由 JobWorker 补发；at-least-once，工作台可提示可能重复 | 在聊天消息里推断是否已发送 |
| 预览冻结 | **已做**：`preview.arguments` 与 `arguments` 不一致直接 409。催办预览冻结订单/收件人，确认后不重查 | 确认时允许模型改参数 |

过门：

- 换货 / 合同确认在 `/workbench` 和原会话都能完成，状态只存一份
- Preview 与 Execute 参数不一致直接拒绝
- 换货 Impact 有 unittest（至少：重复任务、目标 SKU 不存在、跨店）
- 进程在「已生成回复、未发出」处重启，不重跑工具，只补发或标记未知

V5 的 Workflow Runtime 本期 = **现有确认状态机 + Job + Work Item**，不新写一套编排引擎。

---

## 8. S5 上下文 / 会话 / 记忆（S2 之后、S3 之前）

目标：按 [`架构.md`](架构.md) 把「发给模型的内容」做成确定性投影。会话、上下文、记忆、业务状态、审计继续分开。`users` 表已建（身份，不是记忆）。不做 FTS / 向量检索，模型不能自己写长期记忆。

| 项 | 落地 | 不要做成 |
|---|---|---|
| 会话 | **已做**：epoch / 空闲翻篇 / 显式重置；`agent_sessions.user_id`、本 epoch 标题、忙时回「上一条还在处理」 | 群共享模型上下文；重写 `session_events` |
| 上下文组装 | **已做**：系统规则 → 身份/渠道/角色 → 记忆≤500 字 → 摘要≤800 字 → **当前业务对象快照** → 近讯 → 本轮工具 | 把金额、数量、交期、状态当上下文事实 |
| 业务对象快照 | **已做**：从本 epoch 消息和待确认动作抽出采购单号 / `o_id` / SKU / 动作编号；不含数量 | 让模型自己总结「当前在办多少件」 |
| 滚动摘要 | **已做**：落库与回读都消毒。`AGENT_SUMMARY_ENABLED` 默认关。当前业务都是短闭环，工作集已持有单号，开摘要没有收益 | 一上线就开摘要；把摘要当事实来源 |
| 操作员记忆 | **已做**：显式「记住 / 忘记」；按 `user_id`；注入前扫描；只允许已绑定员工。`AGENT_MEMORY_ENABLED` 默认关 | 自动提炼、数字偏好、用记忆覆盖系统规则 |
| 双通道 | **已做**：网页 `/chat` 与钉钉走 `handle_session_command` | 钉钉手写一套、网页靠模型理解「记住」 |

过门：

- 「忘记」之后，新一轮 system 提示不再出现该条
- 摘要里的「待入库 120 件、交期 2026-08-20」不能原样进模型
- 快照只有编号，回答数字仍须重查工具
- 未绑定员工不能写入记忆
- 同会话并发：先到的跑完或超时，后到的不交叉执行；超时回忙，不排队打爆 HTTP

明确后置：独立 `users` / SSO、FTS 历史检索、自动记忆候选、多群 `dingtalk_routes`（S4）、镜像扩源。

---

## 9. S3 ERP Digital Worker（Gate C）

目标：把已验证的「换鞋垫」从员工浏览器 Worker 迁到后端专用账号，仍是类型化命令。

| 项 | 落地 | 不要做成 |
|---|---|---|
| 一个 Digital Worker | **骨架已做**：`erp-ai-procurement`，Playwright 登录/保态/注入换货核心。`.env` 填 `ERP_AI_USERNAME` / `ERP_AI_PASSWORD`，默认门户 `https://www.erp321.com/epaas`。凭证明文不进库 | 多账号调度、数字员工目录 |
| 命令白名单 | **已做**：`erp.exchange_items`、`erp.create_purchase_order` | 模型浏览页面或生成脚本 |
| 执行通道 | **已做**：写并发 1；整批鞋垫试算+写入持有 `DigitalRuntime.exclusive`；`pending_actions` 同时只允许一条鞋垫 `confirmed`（跨进程排队）。换货 / 探测 / 搜 SKU / 商品图片一律由 `DigitalWorkerLoop` 领取，油猴领取口固定返回空。结果未知不重试。钉钉换鞋垫确认后直调同一把锁。**ERP 登录态随 `server.py` 启停**（浏览器 + cookie），不按 09:30–18:30 关浏览器，不固定停在订单页 | 多浏览器各写各的（不要同时开两个 `server.py`） |
| 证据 | **已做**：写前用列表缓存快照；写后只 `Reload` 改过的单（`_ACP` 异步）；对不上不记成功；已是目标 SKU 则跳过改单。JSON 证据落 `files/data/erp-evidence/`。2026-08-19 真机鞋垫批量已走通 | 只信页面返回「成功」 |
| 换货页 | 网页入口隐藏；鞋垫只钉钉确认。执行、探测、搜 SKU、商品图片仍走后端 Playwright；油猴脚本退役 | 两套执行同时写同一张单 |

过门：

- 测试环境走通：候选 `o_id` → Impact → 预览 → 确认 → 后端执行 → 回读
- 命令幂等，重复确认不二次改单
- Digital Worker 停用后不再领新命令
- **其他出库仍关闭**；看板建采购单走 `/purchase` 二次确认 + `erp.create_purchase_order`

---

## 10. S4 业务增量（2026-08-19：先不做）

代发只读已落地。其余单独开闸前不要做：

| 项 | 状态 |
|---|---|
| 代发 | **部分完成**：一张 `YYMMDD-代发.xlsx`，发群并私聊安安；不拆供应商表 |
| 采购草稿 | **已做**：`/spu` `/baihuo` 勾选 → `/purchase` 改单 → 确认写 ERP |
| 多群路由 | 先不做 |
| 准交率 | 口径 + 入库镜像已有；不上页面、不分级 |
| 知识库 | 先不做 |
| 代发采退 / 其他出库 | 先不做（需求第 6 条） |
| 利润看板 | 先不做（需求第 9 条） |

---

## 11. F 商品计划预测看板（下一阶段）

2026-08-19 重设计：**不走旧 F0→F4 旁路清单，不训生产模型，不注册 `forecast_sales`。**
做成固定工作流：镜像出数 → 套员工现有公式 Excel（模板后补）→ 看板展示。
F0 / F0.1 四层（预测 ≠ 建议 ≠ 下单）和「ERP 只对照、不抄 `purchaseQty`」仍有效。细节见 [`预测.md`](预测.md)。

| 步 | 做什么 | 状态 |
|---|---|---|
| 口径 | 四层拆开；库存缺失不兜底；不抄 ERP 建议数 | 已冻结 |
| 销售出库进镜像 | 代理 `sales.out.query` → `realtime_sales_outbounds` | **部分**：已进 60 秒 `sync_all`（失败不拖垮核心源） |
| 售后退货进镜像 | 代理 `aftersales.query` → `realtime_sales_returns` | **部分**：已进 `sync_all`；首次回溯 80 天。代理要 `modified_*`+`start_time`，不要 `archive=true` |
| 现势库存进镜像 | 代理 `inventory.query` → `realtime_inventory` | **部分**：已进 `sync_all` 增量；不动销款可用 `--sync` 按款补 |
| 公式 Excel | 第一份鞋服 SPU 总表 | **部分**：xlsx + `spu_style_snapshot`；已对员工 581 款，日均公式一致；缺货现数等库存/出库补齐。见 [`鞋服SPU表.md`](reports/鞋服SPU表.md) §7 |
| 看板 | **部分**：`/spu` 鞋服读 `spu_style_snapshot`（含 7/15/30 天线下对照）；`/baihuo` 自营百货读 `baihuo_style_snapshot`。百货看板不展示断码/缺码，补货日均×30 | 不推钉钉；不训模型；补货建议列只展示不当结论 |
| 树模型 / `forecast_sales` / 独立 Forecast Service | — | **不做** |

---

## 12. 验收门（进入下一阶段前）

```text
Gate A  身份与会话
        Web/钉钉同一 user_id；同会话串行；未绑定不能写；工具权限拒绝有原因

Gate B  确定性执行
        Work Item 可在 /workbench 处理；Job/Outbox 可恢复；
        Preview 与 Execute 不漂移；换货 Impact 有测试

Gate C  ERP 写入
        一个 Digital Worker；命令幂等；before/after/trace；
        结果未知不重试；正式建单仍关闭

Gate D  RAG / RFQ / 正式建单 / 复杂预测服务
        验证后再开；检索前过滤；报价关键字段结构化；无证据拒答
```

每个已落地场景至少覆盖：Happy Path、缺参/错参、用户隔离、权限边界、幂等、重启恢复、审计可追。钉钉侧另验：同群两人上下文不串、重置不丢业务单、重复 message_id 不重跑、未确认不外发不写 ERP。

每阶段合并前至少：

```bash
python -m py_compile backend/*.py backend/*/*.py scripts/*.py server.py
.venv/bin/python -m unittest tests.test_agent tests.test_identity tests.test_forecast tests.test_forecast_prep tests.test_delivery_reminders tests.test_exchange tests.test_contracts tests.test_dingtalk tests.test_http_auth tests.test_paths tests.test_erp tests.test_insole tests.test_spu_plan tests.test_realtime_mirror
npm run build
```

新增 L1/L2 必须走完整确认流测试，并补黄金回放（`tests/fixtures/golden_dialogues.json`）。改日期或数量口径必须同步根 `README.md`。

---

## 13. 建议的迭代切分

按 4–6 个迭代、每个迭代能合并的粒度：

| 迭代 | 主题 | 可合并的交付 |
|---|---|---|
| 1 | S0 + S1 前半 | **已做**：工具信封、订单候选筛选、`RequestContext`、`user_id` 贯穿审计 |
| 2 | S1 后半 | **已做**：同会话串行、viewer/operator、「我名下」、ToolSpec 字段、usage 落库。Gate A 还差：真实 Web/钉钉同一人联调一眼 |
| 3 | S2 | **已做**：`work_items` + `/workbench`、换货 Impact、Job/Outbox、预览冻结 |
| 4 | S5 | **已做**：上下文组装顺序、业务对象快照、摘要消毒、记忆扫描与 `user_id`、忙时回复 |
| 5 | S3 | **已做**：鞋垫真机批量走通；登录态随服务启停 |
| 6 | S4 | **先不做** |
| 下一步 | F 看板 | 鞋服 SPU 总表已出；看板 / 钉钉后置 |

不要训生产模型，不要注册 `forecast_sales`。

---

## 14. 文档职责（避免再写第三份总架构）

| 文档 | 以后怎么用 |
|---|---|
| **本文** | 唯一执行清单、已拍板、Agent 完成度；做完一项就改状态和进度表 |
| [`架构.md`](架构.md) | 与**当前代码**对齐；落地后改这里 |
| [`预测.md`](预测.md) | 需求、F0 / F0.1 公式语义、ERP 对照、怎么换 Forecaster |
| [`接口参考.md`](接口参考.md) | 聚水潭页面口、供应链代理 API |
| `archive/` | 原文备查（V5、0817 任务书、调研全文）；结论已吸收，不要再当执行清单 |

---

## 15. 代办

**下一阶段（做）：** `--sync` 后对一下总表数字；看板 / 钉钉后置。

这些已拍板「不做」，不要再提、不要趁机开做：

| 项 | 说明 |
|---|---|
| 钉钉通讯录、手机号反查 | 永久不做 |
| 冲突花名入 `users` | 永久不做；`DEFERRED_ALIAS_REVIEWS` 只挡种子 |
| 代发按供应商拆表 | 永久不做 |
| 生产预测模型、`forecast_sales` | 不做 |
| S4：采购草稿 / 多群 / 采退 / 准交率页 / 知识库 / ActionCard | 先不做 |
| C5 普通换货钉钉直写 | 网页已隐藏；有业务场景再开 |
| E4 权限表 / 店铺矩阵 | 测试阶段不做 |
| 看板 / 合同 HTTP 鉴权 | 服务收纳后再看 |
| 滚动摘要改注入角色 | 摘要默认关 |
| 商品小图权威入库 | P2 |
| RetrievalRouter 真正检索 | 有 SOP 之后 |

---

## 16. Agent 完成度

> **改能力必须改本节。** 完成、部分完成、新增、取消或明确不做某一项时，同步改：条目状态、百分比、分组小计、文首总进度、文末「最近变更」。
> 口径与执行顺序仍以仓库根 README.md 和本文前面章节为准；这里只回答「Agent 现在到哪了」。

| 项 | 值 |
|---|---|
| 统计日 | 2026-08-19 |
| **总进度** | **93%** |
| 算法 | 计入「完成 / 部分 / 骨架 / 未做」；「不做」不进分母。条目等权，再按分组加权（见下） |

**怎么改进度**

1. 在对应分组表改该行的状态和 `%`。
2. 重算该分组小计（组内条目等权平均，四舍五入到整数）。
3. 按本节总览权重重算总进度。
4. 在「最近变更」加一行：日期、条目、从 → 到、原因。
5. 新增能力先加一行再标状态，不要只改总进度。

状态：`完成` 已闭环可用 · `部分` 主路径能用但仍有缺口 · `骨架` 代码在、默认关或缺数据 · `未做` · `不做` 本期不做（不扣分）

---

## 总览

| 分组 | 权重 | 小计 | 一句话 |
|---|---:|---:|---|
| A 内核（注册表 / 确认 / 会话 / 双通道） | 20% | 98% | 对话内核可用；网页 `/chat` 先藏，能力走钉钉；网页改花名+密码登录 |
| B 只读查询工具 | 20% | 100% | 采购、订单、国标、缺口都能查 |
| C 写操作与 ERP | 20% | 90% | 合同、鞋垫、看板建采购单能确认后写；鞋垫只钉钉写入，网页换货入口隐藏（C5 不做） |
| D 钉钉与意图 | 20% | 91% | 对话、催办、品控、鞋垫、绑定、Outbox 已落地；多群不做 |
| E 身份与权限 | 10% | 77% | users + 绑定 + webToken；测试阶段不细分权限表 |
| F 预测与订货 | 5% | 90% | 口径冻结；鞋服 `/spu` + 自营百货 `/baihuo`；生产计划表每日重生成并推当月缺口 |
| G 工作台与后置 | 5% | 98% | `/status` 合并待办与运行状态、换货 Impact、Job 租约、Outbox 已落地 |
| **合计** | **100%** | **93%** | `0.20×98 + 0.20×100 + 0.20×90 + 0.20×91 + 0.10×77 + 0.05×90 + 0.05×98` |

---

## A 内核

| ID | 条目 | 状态 | % | 代码 / 说明 |
|---|---|---|---:|---|
| A1 | 工具注册表（名称 / Schema / 风险 / handler） | 完成 | 100 | `backend/agent/tools.py` |
| A2 | L0 直接执行 | 完成 | 100 | `runner._invoke` |
| A3 | L1/L2 pending 确认（谁发起谁确认、幂等一次） | 完成 | 100 | `actions.py`；网页和钉钉「确认 编号」；鞋垫也可只回「确认」；同时只一批 `confirmed`；重启后退回 pending 并私聊补发，5 分钟未确认释放 |
| A4 | 运行 / 工具 / 确认审计 | 完成 | 100 | `audit.py`；过长 JSON 改成合法 `truncated` 对象，不截半截字符串 |
| A5 | 会话隔离、同会话串行、空闲翻篇 | 完成 | 100 | `sessions.py` |
| A6 | 网页 `/chat` | 隐藏 | 90 | 导航不再进 `/chat`，能力走钉钉。网页改花名+密码登录，不再填 Token / 姓名 |
| A7 | CLI 调试（不经 HTTP） | 完成 | 100 | `scripts/run_agent_cli.py`；`--status` 含品控/代发/Job/Outbox/绑定 |
| A8 | 黄金对话回放 | 部分 | 96 | 已落地场景按五类原话补齐；同话题唯一编号可补槽，多个编号追问时列出候选 |
| A9 | 工具结果信封（摘要进模型、全量给页面） | 完成 | 100 | L0 `ok/summary/data/truncated/hint` |
| A10 | 上下文预算 / 会话摘要 | 部分 | 95 | 组装顺序、业务对象快照、摘要消毒已做；摘要默认关。当前短闭环场景无收益 |

**A 小计 98%**（10 条等权：981/10）

---

## B 只读查询工具

| ID | 条目 | 状态 | % | 工具 |
|---|---|---|---:|---|
| B1 | 搜采购单 | 完成 | 100 | `search_purchase_orders` |
| B2 | 采购单头+明细 | 完成 | 100 | `get_purchase_order` |
| B3 | 跟单三档催办清单 | 完成 | 100 | `delivery_reminders`；与交期台账 / 钉钉同一口径 |
| B4 | 看板汇总 | 完成 | 100 | `dashboard_summary` |
| B5 | 商品主数据搜索 | 完成 | 100 | `search_products` |
| B6 | 国标目录状态 | 完成 | 100 | `gb_catalog_status` |
| B7 | 执行标准查询 | 完成 | 100 | `lookup_gb_standards` |
| B8 | 主数据缺口 | 完成 | 100 | `master_data_gaps` |
| B9 | 销售订单定位（o_id） | 完成 | 100 | `search_sales_orders` |
| B10 | 销售订单明细 | 完成 | 100 | `get_sales_order_items` |

**B 小计 100%**

---

## C 写操作与 ERP

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| C1 | 生成采购合同（确认后出 Excel/预览） | 部分 | 95 | `generate_purchase_contract`；soffice 120s / pdftoppm 30s / outputs 30 天；本机已能预览，缺套件时仍失败 |
| C2 | 普通换货 dry-run 登记 | 部分 | 90 | `submit_exchange_dry_run`；确认只登记试算 |
| C3 | 换货页二次确认 + 后端 Playwright 写入 | 部分 | 88 | 网页 `/exchange` 隐藏（与 `/chat` 一样重定向到工作台）。鞋垫只钉钉确认后写入；普通换货接口仍留给 Worker；油猴领取口已关闭 |
| C4 | 抖音换鞋垫：定位 → 清单 → 确认 → 写入 | 部分 | 99 | 处理不再经 LLM；钉钉回「确认」由后端写；写完发【任务完成】；半码舍去小数；查询池含抖音/快手/视频号；确认后不再重扫全库；同页最多 3 路 ChangeItem；写前用列表缓存、写后只刷改过的单（ACP 异步最多 5 路）；写后一次连接回写镜像；待确认只对他人隐藏；重启把未写完写入退回 pending；Delivering 不处理。定位分步查走索引（不 CAST）、60 秒缓存，服务启动预热；慢查询先回「正在查」。抖音另有 09:30–18:30 每小时定时（不经确认；先发清单再总结；可手动触发；档位落盘，重启不重发空跑；有人工待确认不说成没有待处理） |
| C5 | 普通换货从钉钉直接写入（不经换货页） | 不做 | — | `/exchange` 网页已隐藏；无业务场景前不开普通换货钉钉直写 |
| C6 | Digital Worker 领取换货队列 | 部分 | 92 | 换货 / 探测 / 搜 SKU / 商品图片都由后端领取；登录态随 `server.py` 启停；`ERP_AI_ENABLED` 默认开。本机 `ping` 已通。鞋垫确认走同一把锁 |
| C7 | 写入回读 / 预览不一致则停 | 部分 | 88 | 写前用列表缓存；写后只刷新改过的单（`_ACP` 异步）；对不上不记成功。2026-08-19 真机鞋垫批量已走通。截图/trace 仍视本机 Playwright |
| C8 | 备注异常 / 超卖 / 改地址 | 不做 | — | 第一期只做 SKU 换货 |
| C9 | 正式创建采购单 | 部分 | 80 | `/purchase` 二次确认后走 `erp.create_purchase_order`：打开手工下单 `editor.aspx`，调用页面 `_ACP('Confirm')`。`po_date` 用所选日 + 确认当下时分秒。默认待审核。成功后再试生成合同。真机写单尚未在本轮验收 |

**C 小计 90%**（C1–C4、C6、C7、C9：632/7）

---

## D 钉钉与意图

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| D1 | Stream 长连对话 | 部分 | 94 | `DINGTALK_ENABLED`；需 `dingtalk-stream`；员工私聊只确认，管理员私聊全开；@机器人回自我介绍+用法；群里「绑定网页」 |
| D2 | 绑定采购员姓名 | 完成 | 100 | 群内申请；管理员回「同意绑定 / 确认绑定」；有待办时裸「确认」先执行待办；姓名占用不覆盖；双人审批先到先得。群里「绑定成功」@ 申请人 |
| D3 | 「确认 / 取消」 | 完成 | 100 | 可带编号，也可只回「确认」执行当前会话最近一条 |
| D4 | 抖音换鞋垫固定意图 | 完成 | 100 | 查询和处理都直分派；已有待确认再处理会沿用清单；「换鞋垫的动作 / 粘贴单号」可识别；「手动跑抖音鞋垫」走定时工作流 |
| D5 | 每日交期催办推送 | 部分 | 98 | 跟单三档；按钉钉身份拆开只私聊；清单含采购量/待入库；Agent 催办预览冻结订单与收件人 |
| D6 | 品控登记 / 查询 / 日报 | 部分 | 96 | 正则直登 + 可选日报；网页/对话可 L1 关闭/撤销 |
| D7 | 通用意图识别（多业务） | 部分 | 97 | 命令→抽槽→已注册工具/拒答/追问→Agent；当前话题唯一编号可补槽，多个编号列出候选；意图不查权限 |
| D8 | 多群路由（协同群 / 异常群） | 未做 | 0 | S4 |
| D9 | 出站投递台账（失败可补发） | 完成 | 100 | `outbox_messages` + JobWorker 补发；催办走 Outbox |
| D10 | 钉钉卡片轻确认 | 未做 | 0 | 现在是纯文本「确认」，不做 ActionCard |
| D11 | 鞋垫写入完成通知 | 完成 | 100 | 确认后先回「已开始」；写完 `reply_text` 发【任务完成】并 @ 发起人。定时/手动【开始执行】和总结只发群，不私聊 |

**D 小计 91%**

---

## E 身份与权限

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| E1 | `staff_bindings`（姓名 ↔ 钉钉 userId） | 部分 | 90 | 兼容层；可回填 `users.user_id`；绑定同意 /「补发网页账号」/「绑定网页」私信花名+密码，网页登录 30 天 |
| E2 | Web / 钉钉同一 `user_id` | 部分 | 95 | 有 users 则 `usr_`；否则回退钉钉 id / `staff:` |
| E3 | `viewer` / `operator` / `admin` | 部分 | 96 | viewer 不能确认写操作；管理员只认已绑定 userId；`extra_ids` 不再提权；韩立与其他管理员权限相同，只有韩立能改角色。权限表后置 |
| E4 | 用户权限表 / capabilities | 骨架 | 15 | `permissions.py` 已留钩子；表未建。钉钉写操作先要求绑定 |
| E5 | 独立 `users` 表 | 部分 | 85 | schema / 归一化 / 镜像库种子 51 人 / resolve；通讯录不做，继续手绑 |
| E6 | 店铺权限矩阵 | 不做 | — | 本期不做 |
| E7 | 操作员长期记忆 | 部分 | 80 | 「记住 / 忘记」按 `user_id`；注入扫描；仅绑定员工；默认关 |

**E 小计 77%**（E1–E5、E7：461/6）

---

## F 预测与订货

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| F1 | `Forecaster` 接口 + Baseline | 完成 | 100 | `backend/forecast/` |
| F2 | `forecast_demand` 工具 | 部分 | 80 | 已注册；无库存/无模板则不当员工结论 |
| F3 | `order_suggestion` 工具 | 部分 | 80 | 库存缺失直接报错，不用 0 兜底 |
| F4 | 销售出库表进镜像 | 部分 | 95 | 出库已进 `sync_all`；售后 `realtime_sales_returns` 同为可选源（首次 80 天）。代理售后必须带 `modified_*`+`start_time`，不要 `archive=true` |
| F5 | 现势库存表进镜像 | 部分 | 90 | 已进 `sync_all` 近 7 天增量；不动销款用 `--inventory-history` 扫历史 `modified` 窗口回填（代理不转发 `i_id`，按款补数暂不可用） |
| F6 | 拆出 `forecast_sales` + `forecast_id` | 不做 | — | 不训生产模型，不注册 |
| F7 | 业务可用的训练工件 | 不做 | — | 改走公式 Excel，不训线上模型 |
| F8 | F1 旁路模块 | 部分 | 90 | 质量 / 回测 / F0.1 公式代码还在；看板改套员工 Excel |
| F9 | 鞋服 SPU 缺货 / 断码 / 缺码 | 部分 | 95 | 圈定自营无标签款；xlsx + `/spu` 看板（含 7/15/30 天线下对照）；供应商独立成列可筛。勾选建议下单可预览/下载采购单草稿。单款分析先调销量工具再写建议，当天缓存、隔天要重分析。钉钉推送未做 |
| F10 | 生产计划表（第二份公式 Excel） | 部分 | 88 | 镜像标签圈款；订货量月底上传；每天刷新进度。2026-07 起净销=出库−实收退货。钉钉群只发计划表 xlsx。期初快照/上次订货量回填未做 |
| F11 | 自营百货预测面板 | 部分 | 94 | 日均三窗折月；补货日均×30；看板 7/14/30 天 + 7/15/30 渠道；趋势 30 天；供应商独立成列可筛。勾选可出采购单草稿。准交率仍不上页 |

**F 小计 90%**（F1–F5、F8–F11：812/9）

---

## G 工作台与后置（不挡主线）

| ID | 条目 | 状态 | % | 说明 |
|---|---|---|---:|---|
| G1 | `/workbench` 待办列表 | 完成 | 100 | 待办并进 `/status`；`/workbench` 重定向。确认/取消复用 pending_actions；品控关闭/撤销走 `/api/agent/quality/{id}/resolve` 或 `cancel` |
| G2 | 换货 Impact Analysis | 完成 | 100 | `backend/exchange/impact.py`；重复任务 / 目标 SKU 不存在 / 跨店 |
| G3 | 长任务 Job 队列 | 部分 | 88 | `jobs` + 进程内 worker；`running` 300 秒租约过期回收；合同预览仍在确认线程 |
| G4 | 钉钉 Outbox | 完成 | 100 | 与 D9 同一实现 |
| G5 | `supplier_scorecard` | 不做 | — | `RESERVED_TOOLS` 占位 |
| G6 | 代发只读（模板 + Digital Worker 抓取） | 部分 | 95 | `YYMMDD-代发.xlsx` 对齐 8.15；14:00 抓取，抓到后发群并私聊安安；裸价写系统「商品裸价金额」；淘宝揭收货频控停手冷却、解密组件不在不打接口；通知点名未揭开单号；已填表不覆盖 |
| G6b | 采购草稿 | 完成 | 100 | 看板勾选 → `/purchase` 改数量/单头 → 二次确认写 ERP（C9）。鞋服同款列出该款全部商品编码，数量和备注参考上次采购，空数量不写入。采购日期选年月日，写入带当下时分秒；采购员从历史署名下拉；税率随合同票种预选可改。供应商编号/票种与合同共用供应商管理表；付款下拉复用合同 3/7、发货前、到仓后、月度结算，写入 ERP 再映射 payment_method |
| G6c | 知识库 | 不做 | — | S4 |
| G7 | `/status` 运行状态 | 完成 | 100 | 与待办合并为工作台：登录/花名、待办、数据源卡、服务芯片、定时任务 |

**G 小计 98%**（G1–G4、G7 等权；G6 不进本组底）

---

## 最近变更

| 日期 | 条目 | 从 → 到 | 原因 |
|---|---|---|---|
| 2026-08-17 | 本文 | — → 首版 | 建立 Agent 完成度进度表 |
| 2026-08-17 | C4 / D4 / E4 | 未做 → 部分/骨架 | 钉钉换鞋垫：定位清单 + 二次确认 + 串行写入；意图与权限钩子落地 |
| 2026-08-17 | C4 / D3 / D4 | 部分 90 → 95；D3 可裸确认 | 钉钉回「确认」即后端写入并回结果日志；半码按码数舍去小数 |
| 2026-08-17 | D11 | — → 完成 | 鞋垫写入完成后主动发【任务完成】，不再让员工反复查询 |
| 2026-08-17 | C4 | 部分 95（补镜像回写） | 写入成功后按单刷新/回写镜像，定位排除 48 小时内已写入单，避免钉钉重复查出同一批 |
| 2026-08-17 | C4 / D4 | 钉钉清单只展开 5 条 | 待处理/结果只保留单号、状态、鞋码和目标鞋垫，避免一条消息刷屏 |
| 2026-08-17 | D11 | 结果日志加用时 | 【任务完成】带上处理耗时（秒 / 分秒） |
| 2026-08-17 | G6 | 不做 → 骨架 | 代发空白模板落地；抓取走同一套 DigitalRuntime，与采购草稿/知识库拆开 |
| 2026-08-17 | G6 | 骨架 → 部分 55 | 恢复 8.15 全列（隐藏列同样例）；SKU 行写入裸价，供应商/成本价补自商品主数据 |
| 2026-08-17 | G6 | 部分 55 | 模板按 8.15 刷新且不再覆盖当日已填表；缺供应商的 SKU 走页面 GetSku |
| 2026-08-17 | C4 / A3 | 鞋垫写入互斥 | 待确认/写入中的单号不再发给其他人；同时只允许一批 `process_insole_orders` 写入，后来的确认排队 |
| 2026-08-17 | C4 / D4 | 查询池+写入加速 | 快手/视频号与抖音同一策略；确认不再重扫全库、跳过按尺码试算，写入间隔 50ms |
| 2026-08-17 | C4 | 写入并发 | 鞋垫 ChangeItem 同页最多 3 路；核心脚本 0.7.0 版本不符会重注入 |
| 2026-08-17 | C4 / C6 | 登录态保活 | 【任务完成】拆开页/写入；北京时间 09:30–18:30 保活 ERP 登录态，不固定订单页 |
| 2026-08-17 | D5 | 跟单催办 | 每日推送改为已确认未完结、排除返修、10/3/逾期；谁的单只通知谁 |
| 2026-08-17 | D5 | 催办 @ | 应用机器人主动群发不支持 @；改为 sessionWebhook / 自定义 Webhook 的 text + at |
| 2026-08-17 | D5 | 催办私聊 | 已绑定员工改走 `oToMessages/batchSend` 单聊，早上催办不再依赖群 @ |
| 2026-08-17 | B3 | 四波 → 跟单三档 | 交期台账页、Agent 工具与钉钉催办对齐：已确认/排除返修/10·3·逾期 |
| 2026-08-17 | G6 | 部分 55 → 60 | 代导出封装为 L1 工具 `generate_dropship_workbook`，确认后才抓 ERP |
| 2026-08-17 | G1–G4 / D9 | 未做 → 完成/部分 | S2 Gate B：工作台、换货 Impact、Job、Outbox、预览冻结；镜像扩源不做 |
| 2026-08-17 | A10 / E7 | 80/骨架 → 95/部分 | S5：上下文分层、业务对象快照、摘要消毒、记忆扫描与 user_id、忙时回复 |
| 2026-08-17 | D7 / A8 | 骨架/70 → 80 | 意图路由：黄金原话抽槽后直接调工具，缺参追问、越权拒答 |
| 2026-08-17 | C7 | 骨架 40 → 部分 80 | S3：写入前快照、回读 SKU、不一致不记成功、已换过跳过；JSON 证据落 `erp-evidence` |
| 2026-08-17 | C6 | 骨架 45 → 部分 70 | 本机 `ping` 已通订单页；领取队列仍默认关 |
| 2026-08-17 | C3 / C6 | 80/70 → 90/90 | 油猴退役；换货、探测、搜 SKU、商品图片一律走后端 Playwright |
| 2026-08-17 | E5 / E1 / E2 | 未做/部分 → 部分 80/85/95 | users 表、采购员归一化、staff_bindings 兼容；三三与鞋子理单合并 |
| 2026-08-17 | D7 | 80 → 85 | Intent Router 骨架接入 runner；「604264 到货了吗」走 exact_query |
| 2026-08-17 | E5 | 80 → 85 | 镜像库只读扫描 81 署名，自动种子 51 人；待确认 13 条未建用户 |
| 2026-08-17 | E5 | 人工确认韩立=管理员 | 冲突别名只记录，不自动合并 |
| 2026-08-17 | A8 / D7 | 80/85 → 85/88 | 合同缺票种追问；代发/鞋垫黄金原话；payload 有序列名 |
| 2026-08-18 | A8 / D7 / C1 | 85/88/90 → 88/90/95 | 明确 o_id 状态+明细两步 L0；合同预览超时收口记入进度 |
| 2026-08-18 | A8 / D7 | 88/90 → 95/96 | 意图按设计补齐：已注册工具可路由，建单/询价拒答，黄金五类原话，Intent 与权限分开 |
| 2026-08-18 | A8 / D6 / D7 / G6 | 95/90/96/60 → 96/96/97/70 | 话题唯一编号补槽；品控关闭/撤销 L1；代发 14:00 定时默认关 |
| 2026-08-18 | G1 / A7 / D7 | 说明更新 | 工作台可关品控；CLI/status 含调度与 Outbox；多编号追问列出候选 |
| 2026-08-18 | D5 | 95 → 97 | 催办只私聊已绑定；采购量与待入库分列；未绑定不再刷群 |
| 2026-08-18 | C4 / D4 | 95/95 → 96/97 | 利特原话「换鞋垫的动作 / 粘贴单号」进 process；本人待确认不再把自己的单藏掉 |
| 2026-08-18 | A3 / C4 / D4 | 说明 | 鞋垫处理经 LLM 审核后必须调 process；重启把 confirmed 退回 pending，清单不丢 |
| 2026-08-18 | A3 | 说明 | 重启后未确认任务再私聊提醒；5 分钟未确认释放占用 |
| 2026-08-18 | D1 / D2 / E3 | 90/100/80 → 93/100/88 | 员工私聊只收催办和确认；绑定改管理员审批，可批量；管理员私聊全权限 |
| 2026-08-18 | D1 | 说明 | @机器人 /「帮助」改为自我介绍 + 功能用法 |
| 2026-08-18 | D4 | 说明 | DeepSeek 思考模式拒 `tool_choice=required` 时改 auto 重试，避免换鞋垫处理直接失败 |
| 2026-08-18 | C4 / D4 | 96/97 → 97/100 | 换鞋垫处理改回固定分派，不经 LLM；再发「进行处理」沿用已有待确认 |
| 2026-08-18 | G6 | 70 → 88 | 代发定时把当日已填表发钉钉群，不覆盖、不重写收货明文 |
| 2026-08-18 | D2 | 说明 | 绑定审批改回「确认」；同意后群里 @ 申请人报绑定成功 |
| 2026-08-18 | G6 | 88 → 90 | 代发定时除群文件外，再私聊一份给安安 |
| 2026-08-18 | C6 | 90 → 92 | Playwright 登录态随服务启停，不再按 09:30–18:30 关浏览器 |
| 2026-08-18 | G6 | 90 → 92 | 代发 13:30 抓表，14:00 只发送，避免到点才开 ERP |
| 2026-08-18 | A6 / D1 / D2 / E1 / E3 | 90/93/100/85/88 → 94/94/100/88/94 | 管理员不再认显示名；网页用钉钉 20 位码换 webToken；看板/合同鉴权仍后置 |
| 2026-08-18 | D2 / D5 / E3 / G3 / A4 | 说明 + D 90→91 / E 76→77 / G 95→97 | extra_ids 不再提权；绑定改「同意绑定」并处理确认冲突；催办预览冻结；Job 租约；审计不再截半截 JSON；摘要默认关 |
| 2026-08-18 | 文档 | 分篇并入四份现行文档 | 进度表并入本文 §16；架构/身份/接口合并；旧文进 archive |
| 2026-08-18 | 文档 | 归档结论吸收进现行四份 | 会话分层、换单分类、图片决策、调研取舍、黄金回放七条写入 `架构.md` |
| 2026-08-18 | G6 | 说明 | 代发改 13:50 备表；钉钉通知写数据截至时间 |
| 2026-08-18 | G6 | 92 → 93 | 钉钉通知点名揭收货被限流的内部单号；其余齐了才写「都齐了」 |
| 2026-08-18 | C4 / C7 | 97/80 → 98/82 | 鞋垫写前写后回读改一批进页面（5 路），【任务完成】单独报回读耗时 |
| 2026-08-18 | C4 | 说明 | 鞋垫写后回写镜像改一次连接批量更新，不再每单重连 MySQL |
| 2026-08-18 | F 口径 | 说明 | F0 写入 `预测.md`：覆盖=交期、`p10/p50/p90`、出库净销、ERP 只对照；完成度不变 |
| 2026-08-18 | F0.1 | 说明 | 公式语义冻结：合格在途、待发作下界、`not_due`、`k_H` 校准、负净出库；可进 F1，完成度不变 |
| 2026-08-18 | G6 | 93 → 94 | 代发改 14:00 开抓、抓完立刻发群；Excel「商品裸价」对齐系统「商品裸价金额」 |
| 2026-08-18 | F8 | — → 90 | 预测旁路模块先齐：质量/回测/校准/F0.1 公式/ERP 对照；模型不接线上。F 37→46，合计 88→89 |
| 2026-08-19 | C4 / C7 / D11 | 98/82/100 → 98/84/100 | 写前用列表缓存、写后只刷改过的单；Reload 走 ACP 异步；【任务完成】拆写前快照/写后回读 |
| 2026-08-19 | 准交率 / 代发 / 身份 | 说明 | 准交率口径入 README；刃海含仅署「刃海」；代发不拆表；冲突花名明确不要；普通换货钉钉直写需求未定 |
| 2026-08-19 | F 口径 | 说明 | 预测口径归 `预测.md`；推进顺序待重新设计；第一次出数解读归档；`D:\Predict_DATA` 导出已清 |
| 2026-08-19 | A6 / C7 / 准交率 | 94/84 → 96/88 | 网页绑定不再强求共享 Token；鞋垫回读优化真机完成；入库单进镜像（`purchasein`） |
| 2026-08-19 | F / C5 / S4 | 预测改 Excel 工作流 | 出库+库存进镜像为下一步；`forecast_sales`/生产模型/C5/S4/通讯录/拆表永久或先不做。C 79→92，F 46→58，合计 89→92 |
| 2026-08-19 | F4 / F5 / F9 | 未做 → 部分 70/70/60 | 库存/出库镜像表 + 鞋服 SPU CLI；不进 60 秒 `sync_all`。F 58→79，合计 92→93 |
| 2026-08-19 | F9 | 60 → 75 | 按员工列写出「总表」xlsx。F 79→81，合计仍 93 |
| 2026-08-19 | F4 / F5 | 70/70 → 85/85 | 库存/出库进 60 秒 `sync_all`，失败不拖垮核心源。F 81→85 |
| 2026-08-19 | F9 | 说明 | 出表写入 `spu_style_snapshot`（最新一张）；手填列不落库。百分比不变 |
| 2026-08-19 | C4 / D4 / D11 | 98/100/100 → 99/100/100 | 抖音鞋垫 09:30–18:30 每小时定时；先发【开始执行】再总结；「手动跑抖音鞋垫」立刻再跑。C 553→554，合计仍 93 |
| 2026-08-19 | D11 | 说明 | 鞋垫定时/手动通知只发群，不再私聊安安 |
| 2026-08-19 | F9 | 75 → 80 | 对员工 581 款总表：日均公式 480/480 一致；缺货差在现势不是公式。F 85→86，合计仍 93 |
| 2026-08-19 | F4 / F5 | 85/85 → 90/90 | 代理瞬断重试；长回溯逐窗落水位断点续走；库存加 `--inventory-history` 历史窗口回填补不动销款。F 86→87，合计仍 93 |
| 2026-08-19 | F9 | 80 → 85 | 出库 61 天 + 库存 2800 天回填铺满；缺货 48 / 断码 527 / 缺码 65；与员工表对账 440/480 一致。F 87→88，合计仍 93 |
| 2026-08-20 | F9 | 口径核实 | 库存接口一 SKU 一行=页面「实际库存数」（无主仓/全仓之分）；从未入库的缺行 SKU 经示例表核实全为 0，按 0 计断码。断码判定 454/480 一致 |
| 2026-08-20 | F9 | 说明 | 总表版式对齐员工示例（橙表头/条件格式/冻结 Q2）；「是否淘汰」改从标签解析（淘汰/清仓/有升级），不再手填 |
| 2026-08-20 | F9 | 圈定改 | 只出自营且无三标签的款（841→375）；删 25年净销量；加 周环比销量=(近7天−前7天)/前7天。结果表加 sales_prev7 / wow_ratio |
| 2026-08-20 | F9 | 85 → 90 | `/spu` 看板第一版：KPI 卡/品类线筛选/排序/搜索/明细表；`/api/spu/summary` 读结果表、`/api/spu/refresh` 后台重算。F 88→89，合计仍 93 |
| 2026-08-20 | F9 | 说明 | 看板加近 30 天趋势线与「建议下单」（10 倍数取整；商品备注「起订N」按起订量倍数并注明）。百分比不变 |
| 2026-08-20 | F9 | 说明 | 看板加款式明细抽屉（大图+逐日数据+关键指标）与 `/api/spu/analyze` 单款 LLM 解读（数据包进 prompt、无工具、模型不产数字） |
| 2026-08-20 | F9 | 说明 | 去掉「重新计算」按钮；`DailySpuSnapshotScheduler` 每日 09:00 自动重算并写当日 xlsx（`SPU_SNAPSHOT_ENABLED`/`SPU_SNAPSHOT_TIME`）；趋势图加悬停十字线；分析提示词按四段框架重写 |
| 2026-08-20 | F10 | 新增 部分 75 | 第二份公式 Excel「生产计划表」：标签重点产品圈款（137）、订货量读员工表、镜像供数、判定列表内公式、修 11 月错引。见 [`重点产品订货表.md`](reports/重点产品订货表.md)。F 89→87（新条目入分母），合计仍 93 |
| 2026-08-20 | F10 | 说明 | 版式对齐员工原表（分块配色/判定三色条件格式/组头合并/冻结 K5） |
| 2026-08-20 | F10 | 75 → 80 | 进 09:00 每日调度（`SPU_PLAN_SOURCE_XLSX`）：实时库存/在途/净销量/进度/判定每日刷新，需求数重读员工表。F 87→88，合计仍 93 |
| 2026-08-20 | F10 / D1 | 说明 | 订货表更新改上传制：`/spu` 页上传 + 钉钉发 xlsx 给机器人（校验四表→覆盖源文件→后台重生成→回执）。不做本地文件监测 |
| 2026-08-20 | F10 | 口径 | 名册走镜像「重点产品」；两张订货表月底上传定需求；生产计划表每天按镜像库存刷新进度 |
| 2026-08-20 | F10 | 版式 | 历史月列分组折叠（最老整组收起、近两月露售罄率）+ 售罄率 0.9–1.1 警戒，对齐员工原表 |
| 2026-08-20 | F4 / F10 | 90/80 → 95/85 | 售后进镜像（可选源）；生产计划近月净销=出库−实收 `r_qty`（按入库日）。F 88→89，合计仍 93 |
| 2026-08-20 | F10 | 85 → 88 | 当月需补货/及时入库（期初库存/在途 vs 需求，同表内公式）生成后推钉钉群；CLI `--notify`。合计仍 93 |
| 2026-08-20 | F10 | 说明 | 钉钉群改为只发生产计划表 xlsx，不再发需补货清单 |
| 2026-08-20 | F9 | 90 → 92 | 看板「前7天」改称「7~14天」；分析改为工具读销量+同品类/当季经验（不接网页搜索），当天缓存、隔天提示过期。合计仍 93 |
| 2026-08-20 | F9 | 说明 | 分析缓存：summary 带回 + 本机 localStorage，关掉抽屉不再丢；提示词改成判断跟不跟补货建议，禁止复述表上数字 |
| 2026-08-20 | F9 | 说明 | 服装分析纳入资料季节+当月阶段（8月夏季=尾季清货，6–8月加绒=前置备货）；对照优先同季节 |
| 2026-08-20 | F9 | 说明 | 分析材料补近30天形态/库存结构/新品/款年/在途天数/资料毛利率；建议必须写因为①②，禁止同面料 |
| 2026-08-20 | F10 | 说明 | 7 月售罄率：售后入库未覆盖到月末时沿用手贴净销，避免出库当净销偏高 |
| 2026-08-21 | G6 | 94 → 95 | 代发揭淘宝：列表打不开重进门户；解密 SDK 不在不打接口；频控停手冷却 60 秒再揭；已有 oaid 只重试解密。合计仍 93 |
| 2026-08-21 | G7 | 说明 | 看板热缓存自己续热；进页 25 秒内用会话记忆。不上 Redis、不重写 TS。合计仍 93 |
| 2026-08-21 | F9 / F11 | 说明 | 鞋服/百货顶栏渠道条铺满卡片，表列条带随列宽伸展。合计仍 93 |
| 2026-08-21 | G7 | — → 完成 | `/status` 运行状态：数据源、服务、定时任务下次执行。看板过期先出上一份再后台刷新。G 97→98，合计仍 93 |
| 2026-08-21 | F4 / C4 | 说明 | 订单镜像卡在 syncing 时，已成功过的增量 15 分钟后放行，避免鞋垫读到上午的单。合计仍 93 |
| 2026-08-21 | F9 | 92 → 93 | 鞋服看板补 7/15/30 天线下对照（断码/缺码/四窗口日均不动）。F 807→808，合计仍 93 |
| 2026-08-21 | F11 | 说明 | 百货补货改日均×30（鞋服仍×60）。合计仍 93 |
| 2026-08-21 | F11 | 说明 | 百货日均改 (7×4+15×2+30)/3/30；趋势缩到 30 天；表加 7/14 天；渠道和店铺改 7/15/30。合计仍 93 |
| 2026-08-21 | F11 | 说明 | 看板改 30 天对照；趋势 90 天；详情出各窗日销+真实出库店铺；线下加上内部店铺。合计仍 93 |
| 2026-08-21 | F11 | 说明 | 线下改认 ERP 店铺设置 `group_name`（销售渠道按分组展示），不再只靠店名前缀。合计仍 93 |
| 2026-08-21 | C9 / G6b | 不做 / 完成 → 部分 80 / 完成 | `/purchase` 按聚水潭手工下单页改数量后二次确认，Playwright 调 `editor.aspx` 的 `_ACP('Confirm')`，成功再试合同。C 92→91，合计仍 93 |
| 2026-08-21 | G6b / F9 / F11 | 不做 / 93 / 92 → 完成 / 95 / 94 | 商品计划看板勾选建议下单，预览并下载采购单模板；不写聚水潭。F 808→812，合计仍 93 |
| 2026-08-21 | F11 | 90 → 92 | 百货按商品编码/名称出行；销量拆线上/线下（消防/渠道/公安/交警业务部=线下）。合计仍 93 |
| 2026-08-21 | F11 | 88 → 90 | 百货看板去掉断码/缺码（一款一码），卡片改缺货/需补货/进货仓；分析表态少补。合计仍 93 |
| 2026-08-21 | F9/F11 | 说明 | `/spu` `/baihuo` 加进货仓列（`in_qty`），不进总库存，避免货已到仓再按建议下单 |
| 2026-08-21 | F11 | 85 → 88 | 百货单日预测改套鞋服 Excel 七窗等权；分析换百货专属提示词。合计仍 93 |
| 2026-08-21 | F11 | 说明 | 自营百货日均改用 60/90 天出库，不再套鞋服昨天/3/7/15；缺码按 7×日均。鞋服公式不动 |
| 2026-08-21 | F11 | — → 部分 85 | 自营百货预测面板：`/baihuo` 套鞋服 SPU 公式，圈定商品标签；结果表分表以免冲鞋服。F 分母 +1，小计仍 89，合计仍 93 |
| 2026-08-19 | C4 | 说明 | 定时空跑不再把人工待确认说成没有待处理；已跑档位落盘，重启不重播 16:30 空跑 |
| 2026-08-21 | E1 | 说明 | 网页未登录不能进面板；看板/台账/SPU/合同/建单接口要 `webToken`。`/api/health` 仍公开 |
| 2026-08-21 | E1 | 说明 | 已绑定员工补发网页账号：管理员「补发网页账号」或 `issue-web`；绑定同意也会私信花名+新密码。密码不进群 |
| 2026-08-21 | C4 / D3 | 说明 | 钉钉慢查询先回「正在查」；鞋垫定位去掉 CAST、缓存 60 秒并启动预热，缩短收到消息到第一条回复的时间 |
| 2026-08-21 | C3 | 90 → 88 | 换货不在网页执行：还原修改前任务队列页并隐藏入口；鞋垫只走钉钉确认。C 634→632，小计 91→90，合计 93→93 |
| 2026-08-21 | A6 / E1 / C3 / G1 / G7 | 96/88 → 隐藏 90 / 90 | 网页改花名+密码登录（30 天）；去掉 Token/姓名输入；`/exchange` 改鞋垫一件处理；工作台与运行状态合并；`/chat` 先藏（钉钉已覆盖）。A 99→98，合计仍 93 |
| 2026-08-21 | G6b | 说明 | 建采购单主信息：采购日期年月日；采购员历史署名下拉；税率随合同票种预选可改。写入 ERP 的 `po_date` 带确认当下时分秒 |
| 2026-08-21 | G6b | 说明 | 建采购单供应商下拉走本机供应商管理表（编号/票种/税率）；付款优先最近采购，没有再用这家上次写进合同的方式 |
| 2026-08-21 | F9 / F11 / G6b | 说明 | 鞋服/百货品名后跟最近采购供应商；建单付款复用合同 3/7 等选项，写 ERP 再映射 |
| 2026-08-21 | G6b | 说明 | 建单明细：同款列出全部商品编码；数量参考上次采购；没数量的行不写入采购单 |
| 2026-08-21 | F9 / F11 / G6b | 说明 | 鞋服/百货供应商独立成列可筛；建单备注带上次采购单头和明细备注 |
| 2026-08-21 | C4 / D3 | 说明 | 鞋垫定位分步查+20秒缓存；钉钉 Stream 先 ACK 再回消息，避免网页换货和群消息互相堵 |
